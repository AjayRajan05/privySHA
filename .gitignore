__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Virtual environments
venv/
.venv/
.venv-*/
.venv-prod/
.venv-testpypi/
.venv-ci/
.venv-ci-clean/
.venv-pub/
.venv_asha_clean_test/
.venv-mypy-ci/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# Secrets — never commit
.env
.env.*
!.env.example

# Build / packaging
dist/
build/
*.egg-info/
*.egg

# Test / coverage artifacts
.pytest_cache/
.mypy_cache/
.coverage
.coverage.*
coverage.xml
coverage_report.json
htmlcov/
flake8_report*.txt
test_output.txt

# Docs build
site/

# Benchmark outputs (baseline is committed)
benchmarks/output/

# Logs
logs/
*.log

# ANCHOR runtime telemetry (generated locally; opt-in only)
anchor_telemetry.jsonl
**/anchor_telemetry.jsonl

# Local suite / scratch logs (never ship)
_*.txt
_*.xml
_failed_*.txt
_full_suite_*
_suite_*
fake_tests_report.json
find_empty_tests.py
_parse_junit.py
_tmp_*.py
_tmp_*/

# Maintainer-only working notes (optional local)
docs_detail.md

# ---------------------------------------------------------------------------
# Local-only: keep on disk for training / pilots; never push to GitHub
# (library publish / public surface must stay free of these)
# ---------------------------------------------------------------------------

# Detector weights + local datasets
data/
models/
/config/
*.joblib

# Training pipeline + corpora (local maintainer workspace)
training/
asha_training_data_generators/
asha_training_data_generators.zip
llm_augment.py
build_asha_dataset.py
build_best_dataset.py
improve_data_quality.py
Data_README.md.md
To-Do.md

# Local red-team / public corpus staging
jailbreak_llms/
injection_malicious.jsonl
injection_benign.jsonl
sample_data_output/

# Generated model release bundles (rebuild with training/train_hardened.py --pack)
dist/asha-models-*/
dist/asha-models-*.zip

# Local private checkout symlink / clone
.private-artifacts/
asha-private-artifacts/

# Local PyPI upload helpers / tokens (never commit)
.pypi_token
.pypi_token.*
.pypirc
*.pypirc

# Ephemeral release / auth scratch (local only)
.release-notes-*.md
.gh-auth-*.txt
.gh-auth-out.txt
.gh-auth-err.txt

